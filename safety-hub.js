// Discord Safety Hub — expanded account-violation page.
// Paste into the DevTools console on discord.com. Works on an active (normally signed-in) account and on a
// suspended one. A suspended session's token lives only in memory, in AuthenticationStore; this reads it from there.
// With no suspended token the page runs in active-account mode: it reads GET /safety-hub/@me and shows what an
// active Safety Hub supports next to what a suspended one does.
//
// It fetches your own Safety Hub record, decodes every enum the clients ship, and wires up the real
// appeal endpoints — including classifications the built-in page never renders a button for.
//
// Nothing here bypasses moderation: every request is one Discord's own client can make for your account.
// When Discord requests hCaptcha, solve the visible checkbox/challenge. The pending request resumes
// with that solve's token and Discord's challenge metadata. No CAPTCHA tokens are saved or logged.
(() => {
    "use strict";

    const ROOT_ID = "shx-root";
    document.getElementById(ROOT_ID)?.dispatchEvent(new Event("shx-dispose"));
    document.getElementById(ROOT_ID)?.remove();
    const lifetime = new AbortController();
    let disposed = false;
    let activeCaptcha = null;
    let captchaQueue = Promise.resolve();
    let hcaptchaLoading = null;

    // ---------------------------------------------------------------- enums (from the shipped clients)
    const CLASSIFICATION_TYPES = {"0":"UNSPECIFIED","1":"UNKNOWN","100":"UNSOLICITED_PORNOGRAPHY","200":"NONCONSENSUAL_PORNOGRAPHY","210":"GLORIFYING_VIOLENCE","220":"HATE_SPEECH","230":"CRACKED_ACCOUNTS","240":"ILLICIT_GOODS","250":"SOCIAL_ENGINEERING","260":"SELF_GENERATED_CSAM_SAFETY_DISPATCH","270":"UNDERAGE_USER_SAFETY_DISPATCH","280":"CSAM_SAFETY_DISPATCH","286":"CSAM_NON_DEDICATED_GUILD_SAFETY_DISPATCH","290":"DOXXING_MEDIUM_RISK_SAFETY_DISPATCH","296":"DOXXING_MEDIUM_RISK_TIER_2_NON_DEDICATED_GUILD_SAFETY_DISPATCH","300":"DOXXING_HIGH_RISK_SAFETY_DISPATCH","306":"DOXXING_HIGH_RISK_TIER_1_NON_DEDICATED_GUILD_SAFETY_DISPATCH","310":"HARASSMENT_AND_THREATS_SAFETY_DISPATCH","320":"HATE_SPEECH_SAFETY_DISPATCH","326":"HATE_SPEECH_AND_SYMBOLS_NON_DEDICATED_GUILD_SAFETY_DISPATCH","330":"VIOLENCE_GLORIFICATION_SAFETY_DISPATCH","336":"VIOLENCE_GLORIFICATION_NON_DEDICATED_GUILD_SAFETY_DISPATCH","340":"REGULATED_GOODS_HIGH_RISK_SAFETY_DISPATCH","350":"REGULATED_GOODS_MEDIUM_RISK_SAFETY_DISPATCH","356":"MEDIUM_RISK_DANGEROUS_AND_REGULATED_GOODS_NON_DEDICATED_GUILD_SAFETY_DISPATCH","360":"REGULATED_GOODS_LOW_RISK_SAFETY_DISPATCH","370":"FRAUD_SERVICES_SAFETY_DISPATCH","376":"FRAUD_SERVICES_NON_DEDICATED_GUILD_SAFETY_DISPATCH","380":"FINANCIAL_SCAMS_SAFETY_DISPATCH","390":"SEXUAL_HARASSMENT_SAFETY_DISPATCH","400":"NON_CONSENSUAL_SEXUALLY_EXPLICIT_CONTENT_SAFETY_DISPATCH","406":"NON_CONSENSUAL_SEXUALLY_EXPLICIT_CONTENT_NON_DEDICATED_GUILD_SAFETY_DISPATCH","410":"NON_CONSENSUAL_SEXUALLY_SUGGESTIVE_MEDIA_SAFETY_DISPATCH","416":"NON_CONSENSUAL_SEXUALLY_SUGGESTIVE_MEDIA_NON_DEDICATED_GUILD_SAFETY_DISPATCH","420":"TRANSACTION_SCAMS_SAFETY_DISPATCH","500":"CSAM_MEDIUM_CONFIDENCE_HASH_MATCH","501":"CSAM_HIGH_CONFIDENCE_HASH_MATCH","510":"CSAM_MEDIUM_CONFIDENCE_SAFETY_DISPATCH","520":"CHILD_SEXUALIZATION_CONTENT_SAFETY_DISPATCH","526":"CHILD_SEXUALIZATION_CONTENT_NON_DEDICATED_GUILD_SAFETY_DISPATCH","530":"CHILD_LED_SEXUAL_SELF_ENDANGERMENT_SAFETY_DISPATCH","536":"CHILD_LED_SEXUAL_SELF_ENDANGERMENT_NON_DEDICATED_GUILD_SAFETY_DISPATCH","540":"CHILD_ENGAGING_WITH_SEXUALLY_EXPLICIT_CONTENT_SAFETY_DISPATCH","550":"TRIBUTES_AND_LEAKED_ADULT_INTIMATE_MEDIA_SAFETY_DISPATCH","556":"TRIBUTES_AND_LEAKED_ADULT_INTIMATE_MEDIA_NON_DEDICATED_GUILD_SAFETY_DISPATCH","560":"POSTING_AGERESTRICTED_CONTENT_SAFETY_DISPATCH","570":"DENIAL_OF_HUMAN_ATROCITY_SAFETY_DISPATCH","576":"DENIAL_OF_HUMAN_ATROCITY_NON_DEDICATED_GUILD_SAFETY_DISPATCH","580":"SUICIDE_PROMOTION_SAFETY_DISPATCH","586":"SUICIDE_PROMOTION_NON_DEDICATED_GUILD_SAFETY_DISPATCH","590":"SELF_HARM_PROMOTION_SAFETY_DISPATCH","596":"SELF_HARM_PROMOTION_NON_DEDICATED_GUILD_SAFETY_DISPATCH","600":"DRAWN_SEXUALIZED_CHILDREN_SAFETY_DISPATCH","601":"DRAWN_SEXUALIZED_CHILDREN_API","606":"DRAWN_SEXUALIZED_CHILDREN_NON_DEDICATED_GUILD_SAFETY_DISPATCH","610":"CHILD_LED_VIGILANTISM_SAFETY_DISPATCH","620":"INAPPROPRIATE_SEXUAL_INTERACTIONS_WITH_MINORS_NON_GROOMING_SAFETY_DISPATCH","630":"ADMISSION_OF_SEXUAL_MISCONDUCT_WITH_A_CHILD_SAFETY_DISPATCH","640":"CSAM_SOLICITATION_OR_ADMISSION_SAFETY_DISPATCH","641":"CSAM_SOLICITATION_OR_ADMISSION_API","646":"CSAM_SOLICITATION_OR_ADMISSION_NON_DEDICATED_GUILD_SAFETY_DISPATCH","650":"SEXUAL_GROOMING_OF_A_CHILD_AND_CSAM_SOLICITATION_SAFETY_DISPATCH","660":"UNKNOWN_CSAM_FACILITATION_HASH_MATCH","661":"UNKNOWN_CSAM_FACILITATION_SAFETY_DISPATCH","670":"VOYEURISTIC_MEDIA_SAFETY_DISPATCH","676":"VOYEURISTIC_MEDIA_NON_DEDICATED_GUILD_SAFETY_DISPATCH","680":"SEXUAL_DEEPFAKES_SAFETY_DISPATCH","686":"SEXUAL_DEEPFAKES_NON_DEDICATED_GUILD_SAFETY_DISPATCH","690":"MALWARE_AND_MALICIOUS_CONDUCT_SAFETY_DISPATCH","696":"MALWARE_AND_MALICIOUS_CONDUCT_NON_DEDICATED_GUILD_SAFETY_DISPATCH","700":"PHISHING_SAFETY_DISPATCH","710":"IMPERSONATION_HIGH_RISK_SMITE","711":"IMPERSONATION_HIGH_RISK_SAFETY_DISPATCH","712":"IMPERSONATION_HIGH_RISK_ADMIN","720":"PLATFORM_BAN_EVASION_SMITE","730":"GENERIC_CHILD_SELF_ENDANGERMENT_SAFETY_DISPATCH","740":"GENERIC_CSAM_AND_CHILD_SEXUALIZATION_SAFETY_DISPATCH","750":"GENERIC_INAPPROPRIATE_SEXUAL_CONDUCT_WITH_CHILDREN_AND_GROOMING_SAFETY_DISPATCH","760":"GENERIC_FINANCIAL_SCAMS_SAFETY_DISPATCH","770":"GENERIC_FRAUD_SERVICES_SAFETY_DISPATCH","780":"GENERIC_BULLYING_HARASSMENT_AND_THREATS_SAFETY_DISPATCH","790":"GENERIC_HATE_SPEECH_SAFETY_DISPATCH","800":"GENERIC_NON_CONSENSUAL_ADULT_INTIMATE_MEDIA_SAFETY_DISPATCH","810":"GENERIC_SELF_HARM_ENCOURAGEMENT_AND_PROMOTION_SAFETY_DISPATCH","820":"GENERIC_GLORIFICATION_AND_INCITEMENT_OF_VIOLENCE_SAFETY_DISPATCH","830":"GENERIC_CYBERCRIME_SAFETY_DISPATCH","846":"NON_GATED_SERVER_DEDICATED_TO_AGE_RESTRICTED_CONTENT_NON_DEDICATED_GUILD_SAFETY_DISPATCH","850":"VIOLENT_MEDIA_SAFETY_DISPATCH","856":"VIOLENT_MEDIA_NON_DEDICATED_GUILD_SAFETY_DISPATCH","866":"HARASSMENT_AND_NONIMMINENT_AND_OR_NON_PHYSICAL_THREATS_NON_DEDICATED_GUILD_SAFETY_DISPATCH","876":"SALE_OR_PURCHASE_OF_DISCORD_ASSETS_NON_DEDICATED_GUILD_SAFETY_DISPATCH","886":"SALE_OF_PLATFORM_ABUSE_TOOLS_NON_DEDICATED_GUILD_SAFETY_DISPATCH","890":"CHILD_SEXTORTION_FINANCIAL_SAFETY_DISPATCH","895":"CHILD_SEXTORTION_FINANCIAL_GUILD_SAFETY_DISPATCH","896":"CHILD_SEXTORTION_FINANCIAL_NON_DEDICATED_GUILD_SAFETY_DISPATCH","900":"CHILD_SEXTORTION_NON_FINANCIAL_SAFETY_DISPATCH","905":"CHILD_SEXTORTION_NON_FINANCIAL_GUILD_SAFETY_DISPATCH","906":"CHILD_SEXTORTION_NON_FINANCIAL_NON_DEDICATED_GUILD_SAFETY_DISPATCH","910":"ADULT_SEXTORTION_FINANCIAL_SAFETY_DISPATCH","915":"ADULT_SEXTORTION_FINANCIAL_GUILD_SAFETY_DISPATCH","916":"ADULT_SEXTORTION_FINANCIAL_NON_DEDICATED_GUILD_SAFETY_DISPATCH","920":"ADULT_SEXTORTION_NON_FINANCIAL_SAFETY_DISPATCH","925":"ADULT_SEXTORTION_NON_FINANCIAL_GUILD_SAFETY_DISPATCH","926":"ADULT_SEXTORTION_NON_FINANCIAL_NON_DEDICATED_GUILD_SAFETY_DISPATCH","930":"GORE_SAFETY_DISPATCH","936":"SEXUAL_ASSAULT_VIOLENCE_MEDIA_NON_DEDICATED_GUILD_SAFETY_DISPATCH","940":"SALE_OF_MALICIOUS_TOOLS_AND_SERVICES_SAFETY_DISPATCH","945":"SALE_OF_MALICIOUS_TOOLS_AND_SERVICES_GUILD_SAFETY_DISPATCH","946":"SALE_OF_MALICIOUS_TOOLS_AND_SERVICES_NON_DEDICATED_GUILD_SAFETY_DISPATCH","950":"FRAUD_SERVICES_LOW_RISK_SAFETY_DISPATCH","955":"FRAUD_SERVICES_LOW_RISK_GUILD_SAFETY_DISPATCH","956":"FRAUD_SERVICES_LOW_RISK_NON_DEDICATED_GUILD_SAFETY_DISPATCH","960":"FRAUD_SERVICES_MEDIUM_RISK_SAFETY_DISPATCH","965":"FRAUD_SERVICES_MEDIUM_RISK_GUILD_SAFETY_DISPATCH","966":"FRAUD_SERVICES_MEDIUM_RISK_NON_DEDICATED_GUILD_SAFETY_DISPATCH","970":"FRAUD_SERVICES_HIGH_RISK_SAFETY_DISPATCH","975":"FRAUD_SERVICES_HIGH_RISK_GUILD_SAFETY_DISPATCH","976":"GORE_NON_DEDICATED_GUILD_SAFETY_DISPATCH","986":"AGE_RESTRICTED_CONTENT_IN_SERVER_ASSETS_NON_DEDICATED_GUILD_SAFETY_DISPATCH","990":"VIOLENT_EXTREMISM_ORGANIZING_SAFETY_DISPATCH","996":"VIOLENT_EXTREMISM_ORGANIZING_NON_DEDICATED_GUILD_SAFETY_DISPATCH","1006":"VIOLENT_EXTREMISM_SUPPORT_PROMOTION_OR_EMULATION_NON_DEDICATED_GUILD_SAFETY_DISPATCH","1010":"CHILD_SEXUAL_EXPLOITATION_MATERIAL_SAFETY_DISPATCH","1016":"CHILD_SEXUAL_EXPLOITATION_MATERIAL_NON_DEDICATED_GUILD_SAFETY_DISPATCH","1020":"CSAM_CONDUCT_SAFETY_DISPATCH","1026":"CSAM_CONDUCT_NON_DEDICATED_GUILD_SAFETY_DISPATCH","3001":"CSAM_SOLICITATION_OR_ADMISSION_SMITE","3002":"CSAM_SOLICITATION_OR_ADMISSION_GUILD_SMITE","3005":"CSAM_GUILD_SMITE","3010":"SUSPICIOUS_LINK_SMITE","3020":"INVESTMENT_AND_IDENTITY_SCAMS_SMITE","3030":"SPAM_SMITE","3040":"CHARGEBACK_HIGH_RISK_SMITE","3050":"CHARGEBACK_LOW_RISK_SMITE","3065":"NON_CONSENSUAL_SEXUALLY_EXPLICIT_CONTENT_GUILD_SMITE","3075":"INAUTHENTIC_SERVER_MEDIUM_CONFIDENCE_GUILD_SMITE","3085":"MALWARE_AND_MALICIOUS_CONDUCT_SMITE","3090":"MALWARE_AND_MALICIOUS_CONDUCT_GUILD_SMITE","3091":"MALWARE_AND_MALICIOUS_CONDUCT_NON_DEDICATED_GUILD_SMITE","3092":"PHISHING_GUILD_SMITE","3095":"OFF_PLATFORM_BEHAVIORS_GUILD_SMITE","3100":"EARLY_FRAUD_WARNING_LOW_RISK_SMITE","3105":"OFF_PLATFORM_BEHAVIORS_COORDINATED_HARM_GUILD_SMITE","3110":"EARLY_FRAUD_WARNING_HIGH_RISK_SMITE","3115":"INAPPROPRIATE_SEXUAL_INTERACTIONS_WITH_MINORS_NON_GROOMING_SMITE","3120":"SPAM_CEILING_SMITE","3125":"HARASSMENT_AND_NONIMMINENT_AND_OR_NON_PHYSICAL_THREATS_GUILD_SMITE","3130":"VOLUMETRIC_ABUSE_SMITE","3140":"MISLEADING_ABUSE_REPORTS_SMITE","3200":"QUARANTINE_SMITE","3201":"REQUIRE_VERIFIED_PHONE_SMITE","3202":"REQUIRE_REVERIFIED_PHONE_SMITE","3203":"REQUIRE_VERIFIED_EMAIL_SMITE","3204":"REQUIRE_REVERIFIED_EMAIL_SMITE","3205":"USER_INVALIDATE_SESSIONS_SMITE","3206":"USER_TEMP_BAN_SMITE","3207":"USER_FRIEND_REQUEST_DISABLED_SMITE","3208":"USER_LIMITED_ACCESS_FRIEND_REQUEST_TIER4_SMITE","3209":"USERNAME_MANGLE_FULL_SMITE","3210":"GUILD_VOICE_AND_VIDEO_RESTRICTED_SMITE","3211":"USER_WARNED_SPAM_GENERAL_SMITE","3212":"USER_WARNED_SPAM_SMITE","3213":"USER_REMOVE_EMAIL_VERIFICATION_SMITE","3214":"USER_WARNED_SELFBOT_SMITE","3215":"USER_DISABLED_FOR_SUSPICIOUS_ACTIVITY_WITHOUT_NOTIFICATION_SMITE","3216":"REQUIRE_VERIFIED_PHONE_THEN_EMAIL_SMITE","3217":"USER_DISABLED_FOR_SUSPICIOUS_ACTIVITY_SMITE","3218":"SPAMMER_SMITE","3219":"USER_DELETE_SPAM_SILENT_SMITE","3220":"BOT_DELETE_IMPERSONATION_SMITE","3221":"BOT_DELETE_UNSOLICITED_ACCOUNT_CHANGES_SMITE","3222":"BOT_DELETE_TOKEN_SOLICITATION_SMITE","3223":"BOT_DELETE_PLATFORM_BAN_EVASION_SMITE","3224":"BOT_DELETE_CUSTOM_SMITE","3225":"USER_DELETE_SPAM_SMITE","3226":"USER_DELETE_CYBERCRIME_SMITE","3227":"USER_DELETE_SELFBOT_SMITE","3228":"DM_AND_FR_SPAM_SMITE","3230":"SELF_BOTTING_SMITE","3231":"BOT_DELETE_CSAM_SMITE","3232":"BOT_DELETE_NCAIM_SMITE","3233":"APP_GROWTH_HALT_SINGLE_SPAM_SMITE","3234":"APP_GROWTH_HALT_SINGLE_4PA_SMITE","3235":"BOT_DELETE_SPAM_SMITE","3236":"BOT_DELETE_SPAM_SILENT_SMITE","3237":"BAD_BOT_QUARANTINE_SINGLE_SMITE","3238":"BAD_BOT_QUARANTINE_PROPAGATE_SMITE","4010":"PRESENCE_IN_VIOLATIVE_GUILD_MEDIUM_RISK","4100":"OWNER_OF_VIOLATIVE_GUILD_VERY_HIGH_RISK","4110":"OWNER_OF_VIOLATIVE_GUILD_HIGH_RISK","4120":"OWNER_OF_VIOLATIVE_GUILD_MEDIUM_RISK","4130":"OWNER_OF_VIOLATIVE_GUILD_LOW_RISK","4140":"OWNER_OF_VIOLATIVE_GUILD_COPYRIGHT","4150":"OWNER_OF_INAUTHENTIC_ENGAGEMENT_GUILD","5005":"CSAM_GUILD_ADMIN","5006":"CSAM_NON_DEDICATED_GUILD_ADMIN","5010":"CHILD_SEXUALIZATION_CONTENT_ADMIN","5015":"CHILD_SEXUALIZATION_CONTENT_GUILD_ADMIN","5016":"CHILD_SEXUALIZATION_CONTENT_NON_DEDICATED_GUILD_ADMIN","5020":"DRAWN_SEXUALIZED_CHILDREN_ADMIN","5025":"DRAWN_SEXUALIZED_CHILDREN_GUILD_ADMIN","5026":"DRAWN_SEXUALIZED_CHILDREN_NON_DEDICATED_GUILD_ADMIN","5030":"INAPPROPRIATE_SEXUAL_INTERACTIONS_WITH_MINORS_NON_GROOMING_ADMIN","5035":"INAPPROPRIATE_SEXUAL_INTERACTIONS_WITH_MINORS_NON_GROOMING_GUILD_ADMIN","5036":"INAPPROPRIATE_SEXUAL_INTERACTIONS_WITH_MINORS_NON_GROOMING_NON_DEDICATED_GUILD_ADMIN","5040":"ADMISSION_OF_SEXUAL_MISCONDUCT_WITH_A_CHILD_ADMIN","5050":"SEXUAL_GROOMING_OF_A_CHILD_AND_CSAM_SOLICITATION_ADMIN","5055":"SEXUAL_GROOMING_OF_A_CHILD_AND_CSAM_SOLICITATION_GUILD_ADMIN","5060":"SELF_GENERATED_CSAM_SGCSAM_ADMIN","5065":"SELF_GENERATED_CSAM_SGCSAM_GUILD_ADMIN","5070":"CHILD_LED_SEXUAL_SELF_ENDANGERMENT_ADMIN","5075":"CHILD_LED_SEXUAL_SELF_ENDANGERMENT_GUILD_ADMIN","5076":"CHILD_LED_SEXUAL_SELF_ENDANGERMENT_NON_DEDICATED_GUILD_ADMIN","5080":"CHILD_LED_VIGILANTISM_ADMIN","5085":"CHILD_LED_VIGILANTISM_GUILD_ADMIN","5090":"CHILD_ENGAGING_WITH_SEXUALLY_EXPLICIT_CONTENT_ADMIN","5100":"SEXUALLY_EXPLICIT_CONTENT_WITH_IMPLICATIONS_FOR_REAL_WORLD_HARM_ADMIN","5105":"SEXUALLY_EXPLICIT_CONTENT_WITH_IMPLICATIONS_FOR_REAL_WORLD_HARM_GUILD_ADMIN","5106":"SEXUALLY_EXPLICIT_CONTENT_WITH_IMPLICATIONS_FOR_REAL_WORLD_HARM_NON_DEDICATED_GUILD_ADMIN","5110":"NON_CONSENSUAL_SEXUALLY_EXPLICIT_CONTENT_ADMIN","5115":"NON_CONSENSUAL_SEXUALLY_EXPLICIT_CONTENT_GUILD_ADMIN","5116":"NON_CONSENSUAL_SEXUALLY_EXPLICIT_CONTENT_NON_DEDICATED_GUILD_ADMIN","5120":"NON_CONSENSUAL_SEXUALLY_SUGGESTIVE_MEDIA_ADMIN","5125":"NON_CONSENSUAL_SEXUALLY_SUGGESTIVE_MEDIA_GUILD_ADMIN","5126":"NON_CONSENSUAL_SEXUALLY_SUGGESTIVE_MEDIA_NON_DEDICATED_GUILD_ADMIN","5130":"TRIBUTES_AND_LEAKED_ADULT_INTIMATE_MEDIA_ADMIN","5135":"TRIBUTES_AND_LEAKED_ADULT_INTIMATE_MEDIA_GUILD_ADMIN","5136":"TRIBUTES_AND_LEAKED_ADULT_INTIMATE_MEDIA_NON_DEDICATED_GUILD_ADMIN","5140":"VOYEURISTIC_MEDIA_ADMIN","5145":"VOYEURISTIC_MEDIA_GUILD_ADMIN","5146":"VOYEURISTIC_MEDIA_NON_DEDICATED_GUILD_ADMIN","5150":"SEXUAL_DEEPFAKES_ADMIN","5155":"SEXUAL_DEEPFAKES_GUILD_ADMIN","5156":"SEXUAL_DEEPFAKES_NON_DEDICATED_GUILD_ADMIN","5160":"SEXUAL_ASSAULT_VIOLENCE_MEDIA_ADMIN","5165":"SEXUAL_ASSAULT_VIOLENCE_MEDIA_GUILD_ADMIN","5166":"SEXUAL_ASSAULT_VIOLENCE_MEDIA_NON_DEDICATED_GUILD_ADMIN","5170":"SUICIDE_PROMOTION_ADMIN","5175":"SUICIDE_PROMOTION_GUILD_ADMIN","5176":"SUICIDE_PROMOTION_NON_DEDICATED_GUILD_ADMIN","5180":"SELF_HARM_PROMOTION_ADMIN","5185":"SELF_HARM_PROMOTION_GUILD_ADMIN","5186":"SELF_HARM_PROMOTION_NON_DEDICATED_GUILD_ADMIN","5190":"RISK_OF_PHYSICAL_SELF_HARM_ADMIN","5200":"VIOLENT_MEDIA_ADMIN","5205":"VIOLENT_MEDIA_GUILD_ADMIN","5206":"VIOLENT_MEDIA_NON_DEDICATED_GUILD_ADMIN","5210":"GORE_ADMIN","5215":"GORE_GUILD_ADMIN","5216":"GORE_NON_DEDICATED_GUILD_ADMIN","5220":"VIOLENCE_GLORIFICATION_PROMOTION_OR_INCITEMENT_ADMIN","5225":"VIOLENCE_GLORIFICATION_PROMOTION_OR_INCITEMENT_GUILD_ADMIN","5226":"VIOLENCE_GLORIFICATION_PROMOTION_OR_INCITEMENT_NON_DEDICATED_GUILD_ADMIN","5230":"COORDINATION_OR_CREDIBLE_ADMISSION_OF_REAL_WORLD_VIOLENCE_ADMIN","5235":"COORDINATION_OR_CREDIBLE_ADMISSION_OF_REAL_WORLD_VIOLENCE_GUILD_ADMIN","5236":"COORDINATION_OR_CREDIBLE_ADMISSION_OF_REAL_WORLD_VIOLENCE_NON_DEDICATED_GUILD_ADMIN","5240":"HARASSMENT_AND_NONIMMINENT_AND_OR_NON_PHYSICAL_THREATS_ADMIN","5245":"HARASSMENT_AND_NONIMMINENT_AND_OR_NON_PHYSICAL_THREATS_GUILD_ADMIN","5246":"HARASSMENT_AND_NONIMMINENT_AND_OR_NON_PHYSICAL_THREATS_NON_DEDICATED_GUILD_ADMIN","5250":"SEXUAL_HARASSMENT_ADMIN","5260":"SERVER_RAIDS_ADMIN","5265":"SERVER_RAIDS_GUILD_ADMIN","5266":"SERVER_RAIDS_NON_DEDICATED_GUILD_ADMIN","5270":"IMMINENT_THREATS_OF_PHYSICAL_HARM_ADMIN","5275":"IMMINENT_THREATS_OF_PHYSICAL_HARM_GUILD_ADMIN","5276":"IMMINENT_THREATS_OF_PHYSICAL_HARM_NON_DEDICATED_GUILD_ADMIN","5280":"IMMINENT_THREATS_OF_EXTORTION_ADMIN","5285":"IMMINENT_THREATS_OF_EXTORTION_GUILD_ADMIN","5286":"IMMINENT_THREATS_OF_EXTORTION_NON_DEDICATED_GUILD_ADMIN","5290":"DOXXING_HIGH_RISK_TIER_1_ADMIN","5295":"DOXXING_HIGH_RISK_TIER_1_GUILD_ADMIN","5296":"DOXXING_HIGH_RISK_TIER_1_NON_DEDICATED_GUILD_ADMIN","5300":"DOXXING_MEDIUM_RISK_TIER_2_ADMIN","5305":"DOXXING_MEDIUM_RISK_TIER_2_GUILD_ADMIN","5306":"DOXXING_MEDIUM_RISK_TIER_2_NON_DEDICATED_GUILD_ADMIN","5310":"HATE_SPEECH_AND_SYMBOLS_ADMIN","5315":"HATE_SPEECH_AND_SYMBOLS_GUILD_ADMIN","5316":"HATE_SPEECH_AND_SYMBOLS_NON_DEDICATED_GUILD_ADMIN","5320":"DENIAL_OF_HUMAN_ATROCITY_ADMIN","5325":"DENIAL_OF_HUMAN_ATROCITY_GUILD_ADMIN","5326":"DENIAL_OF_HUMAN_ATROCITY_NON_DEDICATED_GUILD_ADMIN","5330":"VIOLENT_EXTREMISM_ORGANIZING_ADMIN","5335":"VIOLENT_EXTREMISM_ORGANIZING_GUILD_ADMIN","5340":"VIOLENT_EXTREMISM_SUPPORT_PROMOTION_OR_EMULATION_ADMIN","5344":"VIOLENT_EXTREMISM_SUPPORT_PROMOTION_OR_EMULATION_GUILD_ADMIN","5346":"VIOLENT_EXTREMISM_SUPPORT_PROMOTION_OR_EMULATION_NON_DEDICATED_GUILD_ADMIN","5350":"HIGH_RISK_DANGEROUS_AND_REGULATED_GOODS_ADMIN","5355":"HIGH_RISK_DANGEROUS_AND_REGULATED_GOODS_GUILD_ADMIN","5360":"MEDIUM_RISK_DANGEROUS_AND_REGULATED_GOODS_ADMIN","5365":"MEDIUM_RISK_DANGEROUS_AND_REGULATED_GOODS_GUILD_ADMIN","5366":"MEDIUM_RISK_DANGEROUS_AND_REGULATED_GOODS_NON_DEDICATED_GUILD_ADMIN","5370":"LOW_RISK_DANGEROUS_AND_REGULATED_GOODS_ADMIN","5375":"LOW_RISK_DANGEROUS_AND_REGULATED_GOODS_GUILD_ADMIN","5376":"LOW_RISK_DANGEROUS_AND_REGULATED_GOODS_NON_DEDICATED_GUILD_ADMIN","5380":"PROMOTING_OR_PARTICIPATING_IN_REAL_WORLD_SEXUAL_SERVICES_ADMIN","5385":"PROMOTING_OR_PARTICIPATING_IN_REAL_WORLD_SEXUAL_SERVICES_GUILD_ADMIN","5386":"PROMOTING_OR_PARTICIPATING_IN_REAL_WORLD_SEXUAL_SERVICES_NON_DEDICATED_GUILD_ADMIN","5390":"ORGANIZING_OR_FACILITATING_ILLICIT_GAMBLING_ADMIN","5395":"ORGANIZING_OR_FACILITATING_ILLICIT_GAMBLING_GUILD_ADMIN","5396":"ORGANIZING_OR_FACILITATING_ILLICIT_GAMBLING_NON_DEDICATED_GUILD_ADMIN","5400":"ORGANIZING_OR_FACILITATING_HUMAN_TRAFFICKING_ADMIN","5405":"ORGANIZING_OR_FACILITATING_HUMAN_TRAFFICKING_GUILD_ADMIN","5410":"USER_UNDER_THE_MINIMUM_AGE_WHERE_THEY_RESIDE_ADMIN","5411":"USER_UNDER_THE_MINIMUM_AGE_WHERE_THEY_RESIDE_API","5420":"POSTING_AGERESTRICTED_CONTENT_ADMIN","5430":"COPYRIGHT_MEDIA_UPLOADS_ATTACHMENTS_512C_ADMIN","5436":"COPYRIGHT_MEDIA_UPLOADS_ATTACHMENTS_512C_NON_DEDICATED_GUILD_ADMIN","5440":"COPYRIGHT_TEXT_OFFPLATFORM_LINK_512D_ADMIN","5446":"COPYRIGHT_TEXT_OFFPLATFORM_LINK_512D_NON_DEDICATED_GUILD_ADMIN","5450":"COPYRIGHT_LIVESTREAMING_512C_ADMIN","5456":"COPYRIGHT_LIVESTREAMING_512C_NON_DEDICATED_GUILD_ADMIN","5460":"TRADEMARK_ADMIN","5465":"TRADEMARK_GUILD_ADMIN","5466":"TRADEMARK_NON_DEDICATED_GUILD_ADMIN","5470":"TRADEMARK_COUNTERFEIT_GOODS_ADMIN","5476":"TRADEMARK_COUNTERFEIT_GOODS_NON_DEDICATED_GUILD_ADMIN","5480":"UNAUTHORIZED_COPYRIGHT_ACCESS_ADMIN","5485":"UNAUTHORIZED_COPYRIGHT_ACCESS_GUILD_ADMIN","5486":"UNAUTHORIZED_COPYRIGHT_ACCESS_NON_DEDICATED_GUILD_ADMIN","5490":"HEALTH_MISINFORMATION_ADMIN","5495":"HEALTH_MISINFORMATION_GUILD_ADMIN","5496":"HEALTH_MISINFORMATION_NON_DEDICATED_GUILD_ADMIN","5500":"CIVIC_MISINFORMATION_ADMIN","5505":"CIVIC_MISINFORMATION_GUILD_ADMIN","5506":"CIVIC_MISINFORMATION_NON_DEDICATED_GUILD_ADMIN","5510":"CIVIC_DISRUPTION_MOBILIZATION_FOR_REAL_WORLD_INTERFERENCE_ADMIN","5515":"CIVIC_DISRUPTION_MOBILIZATION_FOR_REAL_WORLD_INTERFERENCE_GUILD_ADMIN","5516":"CIVIC_DISRUPTION_MOBILIZATION_FOR_REAL_WORLD_INTERFERENCE_NON_DEDICATED_GUILD_ADMIN","5520":"INVESTMENT_AND_IDENTITY_SCAMS_ADMIN","5525":"INVESTMENT_AND_IDENTITY_SCAMS_GUILD_ADMIN","5530":"TRANSACTION_SCAMS_ADMIN","5535":"TRANSACTION_SCAMS_GUILD_ADMIN","5540":"PHISHING_ADMIN","5545":"PHISHING_GUILD_ADMIN","5550":"MALWARE_AND_MALICIOUS_CONDUCT_ADMIN","5555":"MALWARE_AND_MALICIOUS_CONDUCT_GUILD_ADMIN","5556":"MALWARE_AND_MALICIOUS_CONDUCT_NON_DEDICATED_GUILD_ADMIN","5560":"MALWARE_TARGETING_CRITICAL_INFRASTRUCTURE_ADMIN","5565":"MALWARE_TARGETING_CRITICAL_INFRASTRUCTURE_GUILD_ADMIN","5566":"MALWARE_TARGETING_CRITICAL_INFRASTRUCTURE_NON_DEDICATED_GUILD_ADMIN","5570":"MONEY_LAUNDERING_ADMIN","5575":"MONEY_LAUNDERING_GUILD_ADMIN","5580":"FORGERY_ADMIN","5585":"FORGERY_GUILD_ADMIN","5590":"FRAUD_SERVICES_AND_TOOLS_ADMIN","5595":"FRAUD_SERVICES_AND_TOOLS_GUILD_ADMIN","5596":"FRAUD_SERVICES_AND_TOOLS_NON_DEDICATED_GUILD_ADMIN","5600":"MISLEADING_ABUSE_REPORTS_ADMIN","5610":"FRAUDULENT_LEGAL_PROCESS_ADMIN","5620":"HARASSMENT_OF_DISCORDS_SUPPORT_TEAMS_ADMIN","5630":"IMPERSONATION_ADMIN","5635":"IMPERSONATION_GUILD_ADMIN","5640":"PLATFORM_BAN_EVASION_ADMIN","5645":"PLATFORM_BAN_EVASION_GUILD_ADMIN","5650":"OFFPLATFORM_BEHAVIORS_ADMIN","5655":"OFFPLATFORM_BEHAVIORS_GUILD_ADMIN","5660":"CSAM_SOLICITATION_OR_ADMISSION_ADMIN","5665":"CSAM_SOLICITATION_OR_ADMISSION_GUILD_ADMIN","5666":"CSAM_SOLICITATION_OR_ADMISSION_NON_DEDICATED_GUILD_ADMIN","5670":"SALE_OR_PURCHASE_OF_DISCORD_ASSETS_ADMIN","5675":"SALE_OR_PURCHASE_OF_DISCORD_ASSETS_GUILD_ADMIN","5676":"SALE_OR_PURCHASE_OF_DISCORD_ASSETS_NON_DEDICATED_GUILD_ADMIN","5680":"SALE_OR_PURCHASE_OF_ARTIFICIAL_ENGAGEMENT_ADMIN","5685":"SALE_OR_PURCHASE_OF_ARTIFICIAL_ENGAGEMENT_GUILD_ADMIN","5686":"SALE_OR_PURCHASE_OF_ARTIFICIAL_ENGAGEMENT_NON_DEDICATED_GUILD_ADMIN","5695":"SERVER_THAT_HAS_ARTIFICIALLY_INFLATED_MEMBERS_GUILD_ADMIN","5700":"SALE_OF_PLATFORM_ABUSE_TOOLS_ADMIN","5705":"SALE_OF_PLATFORM_ABUSE_TOOLS_GUILD_ADMIN","5706":"SALE_OF_PLATFORM_ABUSE_TOOLS_NON_DEDICATED_GUILD_ADMIN","5716":"NON_GATED_CHANNEL_DEDICATED_TO_AGE_RESTRICTED_CONTENT_ADMIN","5726":"IMPERSONATION_SAFETY_DISPATCH","5727":"INVESTMENT_AND_IDENTITY_SCAMS_SAFETY_DISPATCH","5735":"NON_GATED_SERVER_DEDICATED_TO_AGE_RESTRICTED_CONTENT_ADMIN","5740":"UNKNOWN_CSAM_FACILITATION_ADMIN","5750":"CHARGEBACK_HIGH_RISK_ADMIN","5760":"CHARGEBACK_LOW_RISK_ADMIN","5770":"CHILD_SEXTORTION_FINANCIAL_ADMIN","5775":"CHILD_SEXTORTION_FINANCIAL_GUILD_ADMIN","5776":"CHILD_SEXTORTION_FINANCIAL_NON_DEDICATED_GUILD_ADMIN","5780":"CHILD_SEXTORTION_NON_FINANCIAL_ADMIN","5785":"CHILD_SEXTORTION_NON_FINANCIAL_GUILD_ADMIN","5786":"CHILD_SEXTORTION_NON_FINANCIAL_NON_DEDICATED_GUILD_ADMIN","5790":"ADULT_SEXTORTION_FINANCIAL_ADMIN","5795":"ADULT_SEXTORTION_FINANCIAL_GUILD_ADMIN","5796":"ADULT_SEXTORTION_FINANCIAL_NON_DEDICATED_GUILD_ADMIN","5800":"ADULT_SEXTORTION_NON_FINANCIAL_ADMIN","5805":"ADULT_SEXTORTION_NON_FINANCIAL_GUILD_ADMIN","5806":"ADULT_SEXTORTION_NON_FINANCIAL_NON_DEDICATED_GUILD_ADMIN","5810":"SALE_OF_MALICIOUS_TOOLS_AND_SERVICES_ADMIN","5815":"SALE_OF_MALICIOUS_TOOLS_AND_SERVICES_GUILD_ADMIN","5816":"SALE_OF_MALICIOUS_TOOLS_AND_SERVICES_NON_DEDICATED_GUILD_ADMIN","5820":"FRAUD_SERVICES_LOW_RISK_ADMIN","5825":"FRAUD_SERVICES_LOW_RISK_GUILD_ADMIN","5826":"FRAUD_SERVICES_LOW_RISK_NON_DEDICATED_GUILD_ADMIN","5830":"FRAUD_SERVICES_MEDIUM_RISK_ADMIN","5835":"FRAUD_SERVICES_MEDIUM_RISK_GUILD_ADMIN","5836":"FRAUD_SERVICES_MEDIUM_RISK_NON_DEDICATED_GUILD_ADMIN","5840":"FRAUD_SERVICES_HIGH_RISK_ADMIN","5845":"FRAUD_SERVICES_HIGH_RISK_GUILD_ADMIN","5850":"DENIED_PARTY_SCREENING_ADMIN","5865":"INAUTHENTIC_SERVER_MEDIUM_CONFIDENCE_GUILD_ADMIN","5870":"FRAUDULENT_CHARGE_ADMIN","5880":"HYBRIDIZED_HARM_COORDINATION_OR_FACILITATION_ADMIN","5885":"HYBRIDIZED_HARM_COORDINATION_OR_FACILITATION_GUILD_ADMIN","5890":"UNAUTHORIZED_TRANSACTION_ADMIN","5900":"EARLY_FRAUD_WARNING_LOW_RISK_ADMIN","5910":"EARLY_FRAUD_WARNING_HIGH_RISK_ADMIN","5920":"PRESENCE_IN_HYBRIDIZED_HARM_COORDINATION_OR_FACILITATION_GUILD","5930":"OWNER_OF_HYBRIDIZED_HARM_COORDINATION_OR_FACILITATION_GUILD","5935":"OFF_PLATFORM_BEHAVIORS_COORDINATED_HARM_GUILD_ADMIN","5940":"SPAM_CEILING_ADMIN","5950":"QUEST_REWARD_ABUSE_ADMIN","5960":"OWNER_OF_OFF_PLATFORM_BEHAVIORS_COORDINATED_HARM_GUILD","5970":"PRESENCE_IN_OFF_PLATFORM_BEHAVIORS_COORDINATED_HARM_GUILD","5980":"QUEST_REWARD_ABUSE_POST_WARNING_ADMIN","5990":"BOT_DELETE_IMPERSONATION_ADMIN","6010":"BOT_DELETE_TOKEN_SOLICITATION_ADMIN","6020":"BOT_DELETE_PLATFORM_BAN_EVASION_ADMIN","6030":"BOT_DELETE_CUSTOM_ADMIN","6040":"BOT_DELETE_CSAM_ADMIN","6050":"BOT_DELETE_NCAIM_ADMIN","6060":"BOT_DELETE_SPAM_ADMIN","6070":"BOT_DELETE_SPAM_SILENT_ADMIN","6080":"APP_GROWTH_HALT_SINGLE_SPAM_ADMIN","6090":"APP_GROWTH_HALT_SINGLE_4PA_ADMIN","6100":"BAD_BOT_QUARANTINE_SINGLE_ADMIN","6110":"BAD_BOT_QUARANTINE_PROPAGATE_ADMIN","6120":"LEGALLY_REQUIRED_CONTENT_REMOVAL_ADMIN","6130":"CHILD_SEXUAL_EXPLOITATION_MATERIAL_ADMIN","6135":"CHILD_SEXUAL_EXPLOITATION_MATERIAL_GUILD_ADMIN","6136":"CHILD_SEXUAL_EXPLOITATION_MATERIAL_NON_DEDICATED_GUILD_ADMIN","6140":"CSAM_CONDUCT_ADMIN","6145":"CSAM_CONDUCT_GUILD_ADMIN","6146":"CSAM_CONDUCT_NON_DEDICATED_GUILD_ADMIN","7005":"HIGH_RISK_DANGEROUS_AND_REGULATED_GOODS_GUILD_RETOOL","7015":"MEDIUM_RISK_DANGEROUS_AND_REGULATED_GOODS_GUILD_RETOOL","7025":"LOW_RISK_DANGEROUS_AND_REGULATED_GOODS_GUILD_RETOOL","7035":"ORGANIZING_OR_FACILITATING_ILLICIT_GAMBLING_GUILD_RETOOL","7045":"INVESTMENT_AND_IDENTITY_SCAMS_GUILD_RETOOL","7055":"FRAUD_SERVICES_AND_TOOLS_GUILD_RETOOL","7060":"PLATFORM_BAN_EVASION_RETOOL","7065":"PLATFORM_BAN_EVASION_GUILD_RETOOL","7066":"PLATFORM_BAN_EVASION_NON_DEDICATED_GUILD_RETOOL","7075":"SALE_OR_PURCHASE_OF_DISCORD_ASSETS_GUILD_RETOOL","7085":"SALE_OR_PURCHASE_OF_ARTIFICIAL_ENGAGEMENT_GUILD_RETOOL","7095":"SERVER_THAT_HAS_ARTIFICIALLY_INFLATED_MEMBERS_GUILD_RETOOL","7105":"SALE_OF_PLATFORM_ABUSE_TOOLS_GUILD_RETOOL","7110":"CHILD_LED_SEXUAL_SELF_ENDANGERMENT_RETOOL","7115":"CHILD_LED_SEXUAL_SELF_ENDANGERMENT_GUILD_RETOOL","7116":"CHILD_LED_SEXUAL_SELF_ENDANGERMENT_NON_DEDICATED_GUILD_RETOOL","7120":"CHILD_LED_VIGILANTISM_RETOOL","7125":"CHILD_LED_VIGILANTISM_GUILD_RETOOL","7126":"CHILD_LED_VIGILANTISM_NON_DEDICATED_GUILD_RETOOL","7130":"CHILD_SEXUALIZATION_CONTENT_RETOOL","7135":"CHILD_SEXUALIZATION_CONTENT_GUILD_RETOOL","7136":"CHILD_SEXUALIZATION_CONTENT_NON_DEDICATED_GUILD_RETOOL","7140":"CIVIC_DISRUPTION_MOBILIZATION_FOR_REAL_WORLD_INTERFERENCE_RETOOL","7145":"CIVIC_DISRUPTION_MOBILIZATION_FOR_REAL_WORLD_INTERFERENCE_GUILD_RETOOL","7146":"CIVIC_DISRUPTION_MOBILIZATION_FOR_REAL_WORLD_INTERFERENCE_NON_DEDICATED_GUILD_RETOOL","7150":"CIVIC_MISINFORMATION_RETOOL","7155":"CIVIC_MISINFORMATION_GUILD_RETOOL","7156":"CIVIC_MISINFORMATION_NON_DEDICATED_GUILD_RETOOL","7160":"COORDINATION_OR_CREDIBLE_ADMISSION_OF_REAL_WORLD_VIOLENCE_RETOOL","7165":"COORDINATION_OR_CREDIBLE_ADMISSION_OF_REAL_WORLD_VIOLENCE_GUILD_RETOOL","7166":"COORDINATION_OR_CREDIBLE_ADMISSION_OF_REAL_WORLD_VIOLENCE_NON_DEDICATED_GUILD_RETOOL","7170":"CSAM_RETOOL","7175":"CSAM_GUILD_RETOOL","7176":"CSAM_NON_DEDICATED_GUILD_RETOOL","7180":"DENIAL_OF_HUMAN_ATROCITY_RETOOL","7185":"DENIAL_OF_HUMAN_ATROCITY_GUILD_RETOOL","7186":"DENIAL_OF_HUMAN_ATROCITY_NON_DEDICATED_GUILD_RETOOL","7190":"DRAWN_SEXUALIZED_CHILDREN_RETOOL","7195":"DRAWN_SEXUALIZED_CHILDREN_GUILD_RETOOL","7196":"DRAWN_SEXUALIZED_CHILDREN_NON_DEDICATED_GUILD_RETOOL","7200":"HARASSMENT_AND_NON_IMMINENT_AND_OR_NON_PHYSICAL_THREATS_RETOOL","7205":"HARASSMENT_AND_NON_IMMINENT_AND_OR_NON_PHYSICAL_THREATS_GUILD_RETOOL","7206":"HARASSMENT_AND_NON_IMMINENT_AND_OR_NON_PHYSICAL_THREATS_NON_DEDICATED_GUILD_RETOOL","7210":"HATE_SPEECH_AND_SYMBOLS_RETOOL","7215":"HATE_SPEECH_AND_SYMBOLS_GUILD_RETOOL","7216":"HATE_SPEECH_AND_SYMBOLS_NON_DEDICATED_GUILD_RETOOL","7220":"IMMINENT_THREATS_OF_EXTORTION_RETOOL","7225":"IMMINENT_THREATS_OF_EXTORTION_GUILD_RETOOL","7226":"IMMINENT_THREATS_OF_EXTORTION_NON_DEDICATED_GUILD_RETOOL","7230":"INAPPROPRIATE_SEXUAL_INTERACTIONS_WITH_MINORS_NON_GROOMING_RETOOL","7235":"INAPPROPRIATE_SEXUAL_INTERACTIONS_WITH_MINORS_NON_GROOMING_GUILD_RETOOL","7236":"INAPPROPRIATE_SEXUAL_INTERACTIONS_WITH_MINORS_NON_GROOMING_NON_DEDICATED_GUILD_RETOOL","7240":"MALWARE_AND_MALICIOUS_CONDUCT_RETOOL","7245":"MALWARE_AND_MALICIOUS_CONDUCT_GUILD_RETOOL","7246":"MALWARE_AND_MALICIOUS_CONDUCT_NON_DEDICATED_GUILD_RETOOL","7250":"NON_CONSENSUAL_SEXUALLY_EXPLICIT_CONTENT_RETOOL","7255":"NON_CONSENSUAL_SEXUALLY_EXPLICIT_CONTENT_GUILD_RETOOL","7256":"NON_CONSENSUAL_SEXUALLY_EXPLICIT_CONTENT_NON_DEDICATED_GUILD_RETOOL","7260":"NON_CONSENSUAL_SEXUALLY_SUGGESTIVE_MEDIA_RETOOL","7265":"NON_CONSENSUAL_SEXUALLY_SUGGESTIVE_MEDIA_GUILD_RETOOL","7266":"NON_CONSENSUAL_SEXUALLY_SUGGESTIVE_MEDIA_NON_DEDICATED_GUILD_RETOOL","7270":"NON_GATED_CHANNEL_DEDICATED_TO_AGE_RESTRICTED_CONTENT_RETOOL","7275":"NON_GATED_CHANNEL_DEDICATED_TO_AGE_RESTRICTED_CONTENT_GUILD_RETOOL","7276":"NON_GATED_CHANNEL_DEDICATED_TO_AGE_RESTRICTED_CONTENT_NON_DEDICATED_GUILD_RETOOL","7280":"NON_GATED_SERVER_DEDICATED_TO_AGE_RESTRICTED_CONTENT_RETOOL","7285":"NON_GATED_SERVER_DEDICATED_TO_AGE_RESTRICTED_CONTENT_GUILD_RETOOL","7286":"NON_GATED_SERVER_DEDICATED_TO_AGE_RESTRICTED_CONTENT_NON_DEDICATED_GUILD_RETOOL","7290":"OFF_PLATFORM_BEHAVIORS_RETOOL","7295":"OFF_PLATFORM_BEHAVIORS_GUILD_RETOOL","7296":"OFF_PLATFORM_BEHAVIORS_NON_DEDICATED_GUILD_RETOOL","7300":"ORGANIZING_OR_FACILITATING_HUMAN_TRAFFICKING_RETOOL","7305":"ORGANIZING_OR_FACILITATING_HUMAN_TRAFFICKING_GUILD_RETOOL","7306":"ORGANIZING_OR_FACILITATING_HUMAN_TRAFFICKING_NON_DEDICATED_GUILD_RETOOL","7310":"PROMOTING_OR_PARTICIPATING_IN_REAL_WORLD_SEXUAL_SERVICES_RETOOL","7315":"PROMOTING_OR_PARTICIPATING_IN_REAL_WORLD_SEXUAL_SERVICES_GUILD_RETOOL","7316":"PROMOTING_OR_PARTICIPATING_IN_REAL_WORLD_SEXUAL_SERVICES_NON_DEDICATED_GUILD_RETOOL","7320":"SALE_OF_MALICIOUS_TOOLS_AND_SERVICES_RETOOL","7325":"SALE_OF_MALICIOUS_TOOLS_AND_SERVICES_GUILD_RETOOL","7326":"SALE_OF_MALICIOUS_TOOLS_AND_SERVICES_NON_DEDICATED_GUILD_RETOOL","7330":"SELF_GENERATED_CSAM_SG_CSAM_RETOOL","7335":"SELF_GENERATED_CSAM_SG_CSAM_GUILD_RETOOL","7336":"SELF_GENERATED_CSAM_SG_CSAM_NON_DEDICATED_GUILD_RETOOL","7340":"SELF_HARM_PROMOTION_RETOOL","7345":"SELF_HARM_PROMOTION_GUILD_RETOOL","7346":"SELF_HARM_PROMOTION_NON_DEDICATED_GUILD_RETOOL","7350":"SEXUAL_ASSAULT_VIOLENCE_MEDIA_RETOOL","7355":"SEXUAL_ASSAULT_VIOLENCE_MEDIA_GUILD_RETOOL","7356":"SEXUAL_ASSAULT_VIOLENCE_MEDIA_NON_DEDICATED_GUILD_RETOOL","7360":"SEXUAL_DEEPFAKES_RETOOL","7365":"SEXUAL_DEEPFAKES_GUILD_RETOOL","7366":"SEXUAL_DEEPFAKES_NON_DEDICATED_GUILD_RETOOL","7370":"SEXUAL_GROOMING_OF_A_CHILD_AND_CSAM_SOLICITATION_RETOOL","7375":"SEXUAL_GROOMING_OF_A_CHILD_AND_CSAM_SOLICITATION_GUILD_RETOOL","7376":"SEXUAL_GROOMING_OF_A_CHILD_AND_CSAM_SOLICITATION_NON_DEDICATED_GUILD_RETOOL","7380":"SEXUALLY_EXPLICIT_CONTENT_WITH_IMPLICATIONS_FOR_REAL_WORLD_HARM_RETOOL","7385":"SEXUALLY_EXPLICIT_CONTENT_WITH_IMPLICATIONS_FOR_REAL_WORLD_HARM_GUILD_RETOOL","7386":"SEXUALLY_EXPLICIT_CONTENT_WITH_IMPLICATIONS_FOR_REAL_WORLD_HARM_NON_DEDICATED_GUILD_RETOOL","7390":"SUICIDE_PROMOTION_RETOOL","7395":"SUICIDE_PROMOTION_GUILD_RETOOL","7396":"SUICIDE_PROMOTION_NON_DEDICATED_GUILD_RETOOL","7400":"TRIBUTES_AND_LEAKED_ADULT_INTIMATE_MEDIA_RETOOL","7405":"TRIBUTES_AND_LEAKED_ADULT_INTIMATE_MEDIA_GUILD_RETOOL","7406":"TRIBUTES_AND_LEAKED_ADULT_INTIMATE_MEDIA_NON_DEDICATED_GUILD_RETOOL","7410":"VIOLENCE_GLORIFICATION_PROMOTION_OR_INCITEMENT_RETOOL","7415":"VIOLENCE_GLORIFICATION_PROMOTION_OR_INCITEMENT_GUILD_RETOOL","7416":"VIOLENCE_GLORIFICATION_PROMOTION_OR_INCITEMENT_NON_DEDICATED_GUILD_RETOOL","7420":"VIOLENT_EXTREMISM_ORGANIZING_RETOOL","7425":"VIOLENT_EXTREMISM_ORGANIZING_GUILD_RETOOL","7426":"VIOLENT_EXTREMISM_ORGANIZING_NON_DEDICATED_GUILD_RETOOL","7430":"VIOLENT_EXTREMISM_SUPPORT_PROMOTION_OR_EMULATION_RETOOL","7435":"VIOLENT_EXTREMISM_SUPPORT_PROMOTION_OR_EMULATION_GUILD_RETOOL","7436":"VIOLENT_EXTREMISM_SUPPORT_PROMOTION_OR_EMULATION_NON_DEDICATED_GUILD_RETOOL","7440":"VOYEURISTIC_MEDIA_RETOOL","7445":"VOYEURISTIC_MEDIA_GUILD_RETOOL","7446":"VOYEURISTIC_MEDIA_NON_DEDICATED_GUILD_RETOOL","7450":"FRAUD_SERVICES_LOW_RISK_RETOOL","7455":"FRAUD_SERVICES_LOW_RISK_GUILD_RETOOL","7456":"FRAUD_SERVICES_LOW_RISK_NON_DEDICATED_GUILD_RETOOL","7460":"FRAUD_SERVICES_MEDIUM_RISK_RETOOL","7465":"FRAUD_SERVICES_MEDIUM_RISK_GUILD_RETOOL","7466":"FRAUD_SERVICES_MEDIUM_RISK_NON_DEDICATED_GUILD_RETOOL","7470":"FRAUD_SERVICES_HIGH_RISK_RETOOL","7475":"FRAUD_SERVICES_HIGH_RISK_GUILD_RETOOL","7480":"NON_CONSENSUAL_ADULT_INTIMATE_MEDIA_TAKEDOWN_SAFETY_DISPATCH","7486":"NON_CONSENSUAL_ADULT_INTIMATE_MEDIA_TAKEDOWN_NON_DEDICATED_GUILD_SAFETY_DISPATCH","7490":"NON_CONSENSUAL_ADULT_INTIMATE_MEDIA_TAKEDOWN_ADMIN","7496":"NON_CONSENSUAL_ADULT_INTIMATE_MEDIA_TAKEDOWN_NON_DEDICATED_GUILD_ADMIN","7500":"NON_CONSENSUAL_ADULT_INTIMATE_MEDIA_TAKEDOWN_HASH_MATCH","7505":"NON_CONSENSUAL_ADULT_INTIMATE_MEDIA_TAKEDOWN_NON_DEDICATED_GUILD_HASH_MATCH","9001":"TEST_CLASSIFICATION_LOW","9002":"TEST_CLASSIFICATION_SEVERE","9100":"TEST_GUILD_CLASSIFICATION","9101":"TEST_GUILD_CLASSIFICATION_LOW","9102":"TEST_GUILD_CLASSIFICATION_SEVERE","9200":"TEST_PROVISIONAL_ACCOUNT_CLASSIFICATION"};

    const ACTION_TYPES = {
        0: "BAN", 1: "TEMP_BAN", 2: "GLOBAL_QUARANTINE", 3: "REQUIRE_VERIFICATION", 4: "USER_WARNING",
        5: "USER_SPAMMER", 6: "CHANNEL_SPAM", 7: "MESSAGE_SPAM", 8: "DISABLE_SUSPICIOUS_ACTIVITY",
        9: "LIMITED_ACCESS", 10: "CHANNEL_SCHEDULE_DELETE", 11: "MESSAGE_CONTENT_REMOVAL",
        12: "GUILD_DISABLE_INVITE", 13: "USER_CONTENT_REMOVAL", 14: "USER_USERNAME_MANGLE",
        15: "GUILD_LIMITED_ACCESS", 16: "USER_MESSAGE_REMOVAL", 17: "USER_GENERAL_INFRACTION (unconfirmed)", 20: "GUILD_DELETE",
    };
    const APPEAL_STATUS = { 1: "REVIEW_PENDING", 2: "CLASSIFICATION_UPHELD", 3: "CLASSIFICATION_INVALIDATED" };
    const APPEAL_INGESTION = { 0: "WEBFORM", 1: "AGE_VERIFY", 2: "IN_APP" };
    const APPEAL_ELIGIBILITY = { 1: "DSA_ELIGIBLE", 2: "IN_APP_ELIGIBLE", 3: "AGE_VERIFY_ELIGIBLE", 4: "AGE_VERIFY_GLOBAL_ELIGIBLE" };
    const STANDING = { 100: "ALL_GOOD", 200: "LIMITED", 300: "VERY_LIMITED", 400: "AT_RISK", 500: "SUSPENDED" };
    const MEMBER_TYPE = { 1: "OWNER", 2: "MEMBER" };
    const SIGNALS = {
        0: "I didn't violate the policy", 1: "The decision was too strict or unfair",
        2: "I don't agree with the penalty", 3: "Something else",
    };
    const AGE_METHODS = {
        1: "ID_SELFIE_MATCH", 2: "ID_SCAN", 3: "FACIAL_AGE_ESTIMATION", 4: "BEHAVIORAL_INFERENCE",
        5: "CREDIT_CARD", 6: "EMAIL_DOMAIN", 7: "OS_SIGNAL", 8: "ML_AGE_INFERENCE", 9: "GOOGLE_WALLET",
        10: "NEW_METHOD", 11: "MANUAL_REVIEW", 12: "OS_SIGNAL_CONFIRMED", 13: "TNS_DETERMINATION",
    };
    const AGE_VENDORS = {
        1: "K_ID", 2: "PERSONA", 3: "INCODE", 4: "DISCORD", 5: "GOOGLE_WALLET", 6: "APPLE_APP_STORE", 7: "GOOGLE_PLAY",
    };
    const WEBFORMS = {
        "General appeal": "https://support.discord.com/hc/requests/new?ticket_form_id=360000029731",
        "Spam appeal": "https://support.discord.com/hc/requests/new?ticket_form_id=360000029731&tf_360055270593=__dc.ticket_form-tnsv1_appeals_other_questions__",
        "App / bot appeal": "https://dis.gd/app-appeal",
        "Warning system help": "https://support.discord.com/hc/articles/18210965981847",
        "Underage appeal info": "https://support.discord.com/hc/articles/360041820932",
    };

    // Numeric bands encode which enforcement system produced the classification.
    function band(type) {
        if (type >= 9000) return ["TEST", "Test classification, not a real action"];
        if (type >= 7000) return ["RETOOL", "Applied by hand through Discord's internal admin tooling"];
        if (type >= 6000) return ["BOT/APP", "Bot or application enforcement"];
        if (type >= 5000) return ["ADMIN", "Human admin decision (maybe)"];
        if (type >= 4000) return ["GUILD ASSOCIATION", "Penalty for owning or being present in a violating server"];
        if (type >= 3000) return ["SMITE", "Automated instant action"];
        if (type >= 1000) return ["DISPATCH", "Automated detection pipeline"];
        return ["BASE", "Base policy category / automated dispatch"];
    }
    function suffixNotes(name) {
        const out = [];
        if (/_GUILD_ADMIN$/.test(name)) out.push("action against the server, not you personally");
        if (/_NON_DEDICATED_GUILD/.test(name)) out.push("server wasn't primarily dedicated to the content (lighter variant)");
        if (/_SAFETY_DISPATCH$/.test(name)) out.push("raised by automated detection");
        if (/_SMITE$/.test(name)) out.push("automated instant action");
        if (/_RETOOL$/.test(name)) out.push("actioned via internal admin tooling");
        if (/^OWNER_OF_/.test(name)) out.push("you were treated as the server's owner");
        if (/^PRESENCE_IN_/.test(name)) out.push("you were merely present in the server");
        if (/^BOT_DELETE_/.test(name)) out.push("bot deletion");
        return out;
    }

    // ---------------------------------------------------------------- Discord internals
    const PROBE = "is this a proxy that returns values for any key?";
    const isCatchAll = v => { try { if (v[PROBE] === undefined) return false; Reflect.deleteProperty(v, PROBE); return true; } catch { return true; } };
    const isFn = (v, ...k) => k.every(x => typeof v?.[x] === "function");
    const TESTS = {
        REST: v => isFn(v, "get", "post", "put", "patch", "del"),
        Auth: v => v.constructor?.displayName === "AuthenticationStore" && isFn(v, "getSuspendedUserToken"),
        Users: v => v.constructor?.displayName === "UserStore" && isFn(v, "getCurrentUser"),
    };
    const passes = (k, v) => { try { return v != null && TESTS[k](v); } catch { return false; } };

    function findDiscord() {
        const found = {};
        const V = window.Vencord?.Webpack;
        if (V) {
            try { if (passes("REST", V.Common?.RestAPI)) found.REST = V.Common.RestAPI; } catch { }
            try { const s = V.findStore?.("AuthenticationStore"); if (passes("Auth", s)) found.Auth = s; } catch { }
            try { const s = V.findStore?.("UserStore"); if (passes("Users", s)) found.Users = s; } catch { }
        }
        const need = Object.keys(TESTS).filter(k => !found[k]);
        if (need.length) {
            // Several webpack runtimes share this array; each hands us its own require, so scan them all.
            const requires = [];
            try {
                window.webpackChunkdiscord_app.push([[Symbol("shx")], {}, r => { requires.push(r); }]);
                window.webpackChunkdiscord_app.pop();
            } catch { }
            const consider = v => {
                if (v == null || typeof v !== "object" || v === window || isCatchAll(v)) return;
                for (const k of need) if (!found[k] && passes(k, v)) found[k] = v;
            };
            outer: for (const cache of [...new Set(requires.map(r => r?.c).filter(Boolean))]) {
                for (const id in cache) {
                    const mod = cache[id];
                    if (!mod?.loaded || mod.exports == null) continue;
                    consider(mod.exports);
                    if (typeof mod.exports === "object") {
                        for (const key in mod.exports) {
                            let v; try { v = mod.exports[key]; } catch { continue; }
                            consider(v);
                        }
                    }
                    if (need.every(k => found[k])) break outer;
                }
            }
        }
        return found;
    }

    const { REST, Auth, Users } = findDiscord();
    const state = {
        token: null,        // suspended-user token, memory only
        suspended: false,
        mode: "active",     // "active" (normal sign-in) or "suspended"
        authFound: Auth != null,
        data: null,
        error: null,
        busy: false,
        tab: "overview",
        verifyRequestedAt: null, // when a v2 verification was started on this page; sent as requested_at
    };
    try { state.token = Auth?.getSuspendedUserToken?.() ?? null; } catch { }
    state.suspended = state.token != null;
    state.mode = state.suspended ? "suspended" : "active";

    const currentUser = () => { try { return Users?.getCurrentUser?.() ?? null; } catch { return null; } };
    // An active session's own token, read per request and only for the fetch fallback. Never kept in state.
    const sessionToken = () => { try { const t = Auth?.getToken?.(); return typeof t === "string" && t ? t : null; } catch { return null; } };

    // ---------------------------------------------------------------- API
    const API_BASE = (() => {
        try {
            const endpoint = new URL(window.GLOBAL_ENV.API_ENDPOINT, location.href);
            if (endpoint.protocol !== "https:" || !/^(?:(?:canary|ptb)\.)?discord\.com$/.test(endpoint.hostname)) throw new Error("Invalid API host");
            return endpoint.href.replace(/\/$/, "") + "/v" + (window.GLOBAL_ENV.API_VERSION ?? 9);
        }
        catch { return "https://discord.com/api/v9"; }
    })();

    function captchaBody(error) {
        const value = error?.body ?? error?.response?.body;
        const status = error?.status ?? error?.statusCode ?? error?.response?.status;
        return status >= 400 && status < 500 && value && typeof value === "object" &&
            (value.captcha_key != null || value.captcha_sitekey != null) ? value : null;
    }

    function cancelled() {
        return new DOMException("Verification cancelled. The pending request was not retried.", "AbortError");
    }

    // Prefer Discord's own client (handles auth + base URL). The plain-fetch fallback is enough for suspended
    // endpoints, which authenticate with the token in the body; an active session sends its own session token.
    async function sendRequest(method, url, body, headers) {
        if (REST) {
            const opts = {
                url, oldFormErrors: true, headers, signal: lifetime.signal, retries: 0,
                // Discord otherwise intercepts this error and opens its own modal underneath our UI.
                // A per-request interceptor handles only CAPTCHA errors; other client behavior stays active.
                interceptResponse(response, _retry, reject) {
                    if (!captchaBody(response)) return false;
                    reject({ status: response.status ?? response.statusCode, body: response.body });
                    return true;
                },
            };
            if (body !== undefined) opts.body = body;
            const res = await REST[method](opts);
            return res.body;
        }
        const auth = state.suspended ? null : sessionToken();
        if (!state.suspended && !auth && method !== "get") throw new Error("Discord's REST client wasn't found, and no session token is available for the fallback.");
        const res = await fetch(API_BASE + url, {
            method: method === "del" ? "DELETE" : method.toUpperCase(),
            headers: { ...(body !== undefined ? { "content-type": "application/json" } : {}), ...(auth ? { authorization: auth } : {}), ...headers },
            body: body !== undefined ? JSON.stringify(body) : undefined,
            signal: lifetime.signal,
        });
        const text = await res.text();
        let parsed; try { parsed = JSON.parse(text); } catch { parsed = text; }
        if (!res.ok) throw { status: res.status, body: parsed };
        return parsed;
    }

    async function call(method, url, body) {
        if (disposed) throw cancelled();
        if (!["get", "post", "put", "patch", "del"].includes(method)) throw new Error("Unsupported request method.");
        // The runner must keep authentication and CAPTCHA tokens on Discord's API.
        if (typeof url !== "string" || !url.startsWith("/") || url.startsWith("//") || /[\\\r\n]/.test(url)) {
            throw new Error("Use a Discord API path starting with one /, such as /safety-hub/@me.");
        }
        // Capture the payload before waiting for a human solve; edits cannot change the pending request.
        const payload = body === undefined ? undefined : JSON.parse(JSON.stringify(body));
        let headers = {};
        for (let attempts = 0; ; attempts++) {
            if (disposed) throw cancelled();
            try { return await sendRequest(method, url, payload, headers); }
            catch (error) {
                const challenge = captchaBody(error);
                if (!challenge) throw error;
                if (attempts >= 3) throw new Error("Discord did not accept verification after 3 solves. Try the action again later.");
                // Each retry uses a fresh solve and the metadata from its own error response.
                headers = await solveCaptcha(challenge, method, url, attempts > 0);
            }
        }
    }

    function loadHcaptcha() {
        if (hcaptchaLoading) return hcaptchaLoading;
        if (typeof window.hcaptcha?.render === "function") return Promise.resolve(window.hcaptcha);
        const pending = new Promise((resolve, reject) => {
            const callbackName = "__shxHcaptchaLoaded_" + Math.random().toString(36).slice(2);
            let script, owned = false, poll, timeout, done = false;
            const finish = error => {
                if (done) return;
                done = true;
                clearInterval(poll); clearTimeout(timeout);
                script?.removeEventListener("error", onError);
                lifetime.signal.removeEventListener("abort", onAbort);
                // A late SDK callback after timeout must remain harmless.
                if (window[callbackName]) window[callbackName] = () => { delete window[callbackName]; };
                if (error) { if (owned) script?.remove(); reject(error); }
                else { delete window[callbackName]; resolve(window.hcaptcha); }
            };
            const ready = () => { if (typeof window.hcaptcha?.render === "function") finish(); };
            const onError = () => finish(new Error("hCaptcha could not load. Check your connection or content blocker, then retry."));
            const onAbort = () => finish(cancelled());
            lifetime.signal.addEventListener("abort", onAbort, { once: true });
            timeout = setTimeout(() => finish(new Error("hCaptcha took too long to load. Check your connection, then retry.")), 20000);
            try {
                script = [...document.scripts].find(s => {
                    try { const u = new URL(s.src); return /(^|\.)hcaptcha\.com$/.test(u.hostname) && u.pathname === "/1/api.js"; }
                    catch { return false; }
                });
                if (script) {
                    // Reuse a script Discord is already loading, without replacing its onload callback.
                    script.addEventListener("error", onError, { once: true });
                    poll = setInterval(ready, 50);
                    ready();
                } else {
                    owned = true;
                    window[callbackName] = ready;
                    script = document.createElement("script");
                    script.src = `https://js.hcaptcha.com/1/api.js?render=explicit&recaptchacompat=off&onload=${callbackName}`;
                    script.async = true;
                    script.addEventListener("error", onError, { once: true });
                    document.head.append(script);
                }
            } catch (error) { finish(error); }
        });
        hcaptchaLoading = pending;
        pending.catch(() => { if (hcaptchaLoading === pending) hcaptchaLoading = null; });
        return pending;
    }

    function solveCaptcha(challenge, method, url, repeated) {
        const job = captchaQueue.then(() => {
            if (disposed) throw cancelled();
            if (challenge.captcha_service && challenge.captcha_service !== "hcaptcha") {
                throw new Error(`Discord requested unsupported verification: ${challenge.captcha_service}. Use Discord's own verification screen.`);
            }
            if (typeof challenge.captcha_sitekey !== "string" || !challenge.captcha_sitekey.trim()) {
                throw new Error("Discord requested verification without an hCaptcha site key. Refresh and try again.");
            }
            return showCaptcha(challenge, method, url, repeated);
        });
        captchaQueue = job.catch(() => {});
        return job;
    }

    function showCaptcha(challenge, method, url, repeated) {
        return new Promise((resolve, reject) => {
            const previousFocus = document.activeElement;
            const modal = el("div");
            modal.id = "shx-captcha";
            modal.innerHTML = `<div class="shx-captcha-panel" role="dialog" aria-modal="true" aria-labelledby="shx-captcha-title" aria-describedby="shx-captcha-status">
                <h2 id="shx-captcha-title">Verify to continue</h2>
                <p>Complete hCaptcha below. Your request will continue automatically after verification.</p>
                <p class="shx-captcha-request"></p>
                <p id="shx-captcha-status" role="status" aria-live="polite"></p>
                <div class="shx-captcha-widget"></div>
                <div class="shx-captcha-actions"><button type="button" class="shx-captcha-retry" hidden>Reload hCaptcha</button><button type="button" class="shx-captcha-cancel">Cancel request</button></div>
            </div>`;
            modal.querySelector(".shx-captcha-request").textContent = `${method.toUpperCase()} ${url}`;
            const status = modal.querySelector("#shx-captcha-status");
            const container = modal.querySelector(".shx-captcha-widget");
            const retry = modal.querySelector(".shx-captcha-retry");
            let sdk, widgetId = null, settled = false, generation = 0;
            const elevated = new Map();

            // hCaptcha puts its puzzle in a separate body-level portal. Raise only this widget's portal
            // above both fullscreen layers, including when the SDK changes the portal's inline styles.
            const liftChallenge = () => {
                if (!root.isConnected) { dispose(); return; }
                if (widgetId == null || settled) return;
                for (const frame of document.querySelectorAll("iframe[src]")) {
                    let source;
                    try { source = new URL(frame.src); } catch { continue; }
                    if (!/(^|\.)hcaptcha\.com$/.test(source.hostname)) continue;
                    const params = new URLSearchParams(source.hash.slice(1) || source.search);
                    if (params.get("id") !== String(widgetId) || params.get("frame") !== "challenge") continue;
                    if (modal.contains(frame)) continue;
                    let portal = frame;
                    while (portal.parentElement && portal.parentElement !== document.body) portal = portal.parentElement;
                    if (portal.parentElement !== document.body || portal === root) continue;
                    if (!elevated.has(portal)) elevated.set(portal, {
                        z: portal.style.getPropertyValue("z-index"), zPriority: portal.style.getPropertyPriority("z-index"),
                        position: portal.style.getPropertyValue("position"), positionPriority: portal.style.getPropertyPriority("position"),
                    });
                    if (getComputedStyle(portal).position === "static") portal.style.setProperty("position", "relative", "important");
                    if (portal.style.getPropertyValue("z-index") !== "2147483647" || portal.style.getPropertyPriority("z-index") !== "important") {
                        portal.style.setProperty("z-index", "2147483647", "important");
                    }
                }
            };
            const observer = new MutationObserver(liftChallenge);
            const removeWidget = () => {
                const id = widgetId;
                widgetId = null;
                if (id != null) { try { sdk.remove(id); } catch { } }
                container.replaceChildren();
            };
            const finish = (error, token) => {
                if (settled) return;
                settled = true;
                generation++;
                observer.disconnect();
                removeWidget();
                for (const [portal, saved] of elevated) {
                    portal.style.setProperty("z-index", saved.z, saved.zPriority);
                    portal.style.setProperty("position", saved.position, saved.positionPriority);
                }
                modal.remove();
                root.inert = false;
                activeCaptcha = null;
                if (!disposed && previousFocus?.isConnected) previousFocus.focus();
                if (error) reject(error);
                else {
                    const headers = { "X-Captcha-Key": token };
                    if (challenge.captcha_rqtoken != null) headers["X-Captcha-Rqtoken"] = challenge.captcha_rqtoken;
                    if (challenge.captcha_session_id != null) headers["X-Captcha-Session-Id"] = challenge.captcha_session_id;
                    resolve(headers);
                }
            };
            const start = async () => {
                const current = ++generation;
                removeWidget();
                retry.hidden = true;
                status.textContent = "Loading hCaptcha…";
                try {
                    sdk = await loadHcaptcha();
                    if (settled || current !== generation) return;
                    if (challenge.captcha_rqdata != null && typeof sdk.setData !== "function") {
                        throw new Error("This hCaptcha SDK cannot attach Discord's challenge data. Reload Discord and try again.");
                    }
                    const isCurrent = () => !settled && current === generation;
                    const notify = message => { if (isCurrent()) { status.textContent = message; retry.hidden = false; } };
                    let accepting = true;
                    const invalidate = message => { accepting = false; notify(message); };
                    widgetId = sdk.render(container, {
                        sitekey: challenge.captcha_sitekey,
                        size: container.clientWidth < 304 ? "compact" : "normal",
                        theme: "dark",
                        callback: token => {
                            if (!isCurrent() || !accepting) return;
                            if (typeof token !== "string" || !token.trim()) { notify("hCaptcha returned an empty response. Reload it and try again."); return; }
                            finish(null, token);
                        },
                        "open-callback": liftChallenge,
                        "close-callback": () => notify("Challenge closed. Select the checkbox to try again, or cancel the request."),
                        "expired-callback": () => invalidate("Verification expired. Reload hCaptcha to try again."),
                        "chalexpired-callback": () => invalidate("Challenge expired. Reload hCaptcha to try again."),
                        "error-callback": code => invalidate(`hCaptcha error: ${String(code)}. Check your connection, then reload hCaptcha.`),
                    });
                    if (challenge.captcha_rqdata != null) sdk.setData(widgetId, { rqdata: challenge.captcha_rqdata });
                    // Use a visible checkbox even when the response suggests invisible mode. Its click
                    // starts the provider's normal human verification flow with rqdata already attached.
                    status.textContent = repeated ? "Discord requested a fresh verification. Select the checkbox below." : "Select the checkbox below to verify.";
                    liftChallenge();
                } catch (error) {
                    if (settled || current !== generation) return;
                    generation++;
                    removeWidget();
                    status.textContent = String(error?.message ?? error);
                    retry.hidden = false;
                }
            };
            activeCaptcha = { cancel: () => finish(cancelled()) };
            modal.querySelector(".shx-captcha-cancel").onclick = activeCaptcha.cancel;
            retry.onclick = start;
            root.inert = true;
            document.body.append(modal);
            observer.observe(document.body, { childList: true, subtree: true, attributes: true, attributeFilter: ["src", "style"] });
            modal.querySelector(".shx-captcha-cancel").focus();
            start();
        });
    }

    // These endpoints authenticate with the suspended token in the body; an active session has none to send.
    function suspendedCall(method, url, body = {}) {
        if (!state.suspended) return Promise.reject(new Error(`${url} needs a suspended session. This is an active account, so there is no suspended token to send.`));
        return call(method, url, { token: state.token, ...body });
    }
    // These use the normal login; a suspended session has none to send.
    function activeCall(method, url, body) {
        if (state.suspended) return Promise.reject(new Error(`${url} needs a normally signed-in account. This is a suspended session.`));
        return call(method, url, body);
    }

    // Request bodies match what Discord's mobile client sends (Wumpus-Central/discord-mobile-datamining).
    const api = {
        fetchData: () => state.suspended
            ? call("post", "/safety-hub/suspended/@me", { token: state.token })
            : call("get", "/safety-hub/@me"),
        appeal: (id, signal, userInput) => state.suspended
            ? call("put", `/safety-hub/suspended/request-review/${id}`, { signal, user_input: userInput, token: state.token })
            : call("put", `/safety-hub/request-review/${id}`, { signal, user_input: userInput }),
        ageMethods: () => state.suspended
            ? call("post", "/age-verification/suspended/methods", { token: state.token })
            : call("get", "/age-verification/methods"),
        ageMethodsV2: () => state.suspended
            ? call("post", "/age-verification/suspended/methods/v2", { token: state.token })
            : call("get", "/age-verification/methods/v2"),
        // Active account
        verifyAge: (method, vendor) => activeCall("post", "/age-verification/verify", { method, vendor }),
        verifyAgeV2: (method, vendor) => activeCall("post", "/age-verification/verify/v2", { method, vendor }),
        ageCheck: () => activeCall("get", "/users/@me/age-verification/check"),
        // Suspended session
        requestVerification: (classificationId, method) =>
            suspendedCall("post", "/safety-hub/suspended/request-verification", { from_classification_id: classificationId, method }),
        requestVerificationV2: async (method, vendor) => {
            const res = await suspendedCall("post", "/safety-hub/suspended/request-verification/v2", { method, vendor });
            state.verifyRequestedAt = new Date().toISOString();
            return res;
        },
        checkVerification: () => suspendedCall("post", "/safety-hub/suspended/check-verification"),
        // The app sends when verification was requested; the decompiled client doesn't show the format, so
        // this sends the ISO time recorded when v2 verification started here, or now.
        checkVerificationV2: (requestedAt = state.verifyRequestedAt ?? new Date().toISOString()) =>
            suspendedCall("post", "/safety-hub/suspended/check-verification/v2", { requested_at: requestedAt }),
        // Either session, routed to the endpoint that session uses
        manualReview: () => state.suspended
            ? suspendedCall("post", "/age-verification/suspended/manual-review")
            : activeCall("post", "/age-verification/manual-review"),
        startVerification: (method, vendor, classificationId = null) => state.suspended
            ? api.requestVerification(classificationId, method)
            : api.verifyAge(method, vendor),
        startVerificationV2: (method, vendor) => state.suspended ? api.requestVerificationV2(method, vendor) : api.verifyAgeV2(method, vendor),
        checkStatus: () => state.suspended ? api.checkVerification() : api.ageCheck(),
        raw: (method, url, body) => call(method, url, body),
    };

    // ---------------------------------------------------------------- helpers
    const esc = s => String(s ?? "").replace(/[&<>"']/g, c => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[c]));
    const el = (tag, cls, html) => { const n = document.createElement(tag); if (cls) n.className = cls; if (html != null) n.innerHTML = html; return n; };
    const pretty = v => esc(JSON.stringify(v, null, 2));
    let uid = 0;
    const nextId = prefix => `shx-${prefix}-${++uid}`;

    // A few outline icons on a 24px grid, inlined so nothing loads from a third-party host.
    const ICONS = {
        check: '<path d="M20 6 9 17l-5-5"/>',
        x: '<path d="M18 6 6 18"/><path d="m6 6 12 12"/>',
        minus: '<path d="M5 12h14"/>',
        alert: '<path d="M12 8v4"/><path d="M12 16h.01"/><circle cx="12" cy="12" r="10"/>',
        warn: '<path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3"/><path d="M12 9v4"/><path d="M12 17h.01"/>',
        info: '<circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/>',
        clock: '<circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/>',
        lock: '<rect width="18" height="11" x="3" y="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/>',
        chevron: '<path d="m6 9 6 6 6-6"/>',
        external: '<path d="M15 3h6v6"/><path d="M10 14 21 3"/><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/>',
        refresh: '<path d="M3 12a9 9 0 0 1 9-9 9.75 9.75 0 0 1 6.74 2.74L21 8"/><path d="M21 3v5h-5"/><path d="M21 12a9 9 0 0 1-9 9 9.75 9.75 0 0 1-6.74-2.74L3 16"/><path d="M8 16H3v5"/>',
        shieldCheck: '<path d="M20 13c0 5-3.5 7.5-7.66 8.95a1 1 0 0 1-.67-.01C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.24-2.72a1.17 1.17 0 0 1 1.52 0C14.51 3.81 17 5 19 5a1 1 0 0 1 1 1z"/><path d="m9 12 2 2 4-4"/>',
    };
    const icon = (name, cls = "") => `<svg class="ico${cls ? " " + cls : ""}" viewBox="0 0 24 24" aria-hidden="true" focusable="false">${ICONS[name] ?? ""}</svg>`;
    const tag = (text, tone = "", ico = "") => `<span class="tag${tone ? " " + tone : ""}">${ico ? icon(ico) : ""}${esc(text)}</span>`;
    const yesNo = v => v ? `<span class="yes">${icon("check")}Yes</span>` : `<span class="no">${icon("minus")}No</span>`;
    const tokenHint = () => state.token ? `${esc(state.token.slice(0, 6))}… (${state.token.length} chars)` : "none";
    const result = (ok, label, value) => `<div class="res ${ok ? "ok" : "bad"}"><div class="res-h">${icon(ok ? "check" : "warn")}${esc(label)}</div><pre>${pretty(value ?? "(empty response)")}</pre></div>`;
    const failure = (label, e) => result(false, `${label} failed${e?.status ? " · HTTP " + e.status : ""}`, e?.body ?? String(e?.message ?? e));
    const pending = label => `<div class="res pending"><div class="res-h">${icon("refresh")}${esc(label)}…</div></div>`;
    const safeLink = url => { try { return /^https?:$/.test(new URL(url).protocol) ? url : null; } catch { return null; } };

    // Discord's own avatar when the current user is known, else one of its default avatars.
    function avatarHTML(size) {
        const user = currentUser();
        const name = state.data?.username ?? user?.username ?? "";
        let src = null;
        try { src = user?.getAvatarURL?.(null, size * 2) ?? null; } catch { }
        if (!src && user?.id && user?.avatar) src = `https://cdn.discordapp.com/avatars/${user.id}/${user.avatar}.webp?size=${size * 2}`;
        if (!src) {
            let index = 0;
            try { if (user?.id) index = Number((BigInt(user.id) >> 22n) % 6n); } catch { }
            src = `https://cdn.discordapp.com/embed/avatars/${index}.png`;
        }
        return `<div class="avatar${state.suspended ? " suspended" : ""}"><span class="ini">${esc((name.trim()[0] ?? "?").toUpperCase())}</span><img alt="" src="${esc(src)}"><span class="pres"></span></div>`;
    }

    function rel(iso) {
        if (iso == null) return { text: "no expiry set", expired: true, note: "null — the client sorts this as already expired, so the built-in page hides it" };
        const t = new Date(iso).getTime();
        if (isNaN(t)) return { text: String(iso), expired: false };
        const ms = t - Date.now(), abs = Math.abs(ms);
        const d = Math.floor(abs / 86400000), y = Math.floor(d / 365), mo = Math.floor((d % 365) / 30);
        const span = y > 0 ? `${y}y ${mo}mo` : d > 30 ? `${mo}mo` : `${d}d`;
        return { text: ms > 0 ? `in ${span}` : `${span} ago`, expired: ms <= 0, when: new Date(t).toLocaleString() };
    }

    const typeName = t => CLASSIFICATION_TYPES[t] ?? null;
    const actionName = t => ACTION_TYPES[t] ?? null;
    const classificationsOf = d => [...(d?.classifications ?? []), ...(d?.guild_classifications ?? [])];

    // The client's own rule for whether the in-app appeal flow is offered.
    function appealability(c, top) {
        const reasons = [];
        if (c.appeal_status != null) reasons.push(`already appealed — ${APPEAL_STATUS[c.appeal_status.status] ?? c.appeal_status.status}`);
        if (top?.is_appeal_eligible === false) reasons.push("account not appeal-eligible");
        if (c.appeal_ingestion_type != null && c.appeal_ingestion_type !== 2) reasons.push(`ingestion type is ${APPEAL_INGESTION[c.appeal_ingestion_type] ?? c.appeal_ingestion_type}, not IN_APP`);
        if (c.is_coppa) reasons.push("COPPA classification");
        if (c.is_spam) reasons.push("spam classification (routes to the spam webform)");
        if (c.is_developer_classification) reasons.push("developer classification");
        return { ok: reasons.length === 0, reasons };
    }

    const STANDING_LEVELS = [
        { state: 100, label: "All good", tone: "ok", title: "Your account is in good standing", text: "Nothing on record is limiting this account right now." },
        { state: 200, label: "Limited", tone: "warn", title: "Your account is limited", text: "Some features may be restricted while active violations remain." },
        { state: 300, label: "Very limited", tone: "orange", title: "Your account is very limited", text: "More features may be restricted while active violations remain." },
        { state: 400, label: "At risk", tone: "bad", title: "Your account is at risk", text: "Another violation could lead to a suspension." },
        { state: 500, label: "Suspended", tone: "bad", title: "Your account is suspended", text: "You can appeal a violation or verify your age to request a review." },
    ];
    const APPEAL_LABEL = { 1: "Review pending", 2: "Upheld", 3: "Overturned" };
    // SuspendedAgeCheckStatus in the mobile client's SafetyHub constants.
    const VERIFY_STATUS = { 1: "PENDING", 2: "UNBANNED", 3: "VERIFIED_OTHER_VIOLATIONS_REMAIN", 4: "UNDERAGE", 5: "UNDERAGE_MANUAL_REVIEW" };
    const TABS = [
        ["overview", "Account Standing"],
        ["violations", "Violations"],
        ["capabilities", "What's Supported"],
        ["age", "Age Verification"],
        ["developer", "Developer"],
    ];

    // ---------------------------------------------------------------- rendering
    // Styled after Discord's User Settings screen. Colors come from Discord's own theme tokens, so the page
    // follows the user's theme; the fallbacks are the dark theme's values for pages without them.
    const R = "#" + ROOT_ID;
    const root = el("div"); root.id = ROOT_ID;
    root.addEventListener("shx-dispose", dispose, { once: true });
    root.style.colorScheme = document.documentElement.classList.contains("theme-light") ? "light" : "dark";
    root.innerHTML = `<style>
${R}{--c-side:var(--background-base-lowest,#2c2d32);--c-bg:var(--background-base-lower,#323339);--c-card:var(--card-background-default,var(--background-surface-high,#393a41));--c-code:var(--background-base-lowest,#2c2d32);
--c-text:var(--text-default,#f3f3f4);--c-strong:var(--text-strong,#fff);--c-subtle:var(--text-subtle,#c5c6ca);--c-muted:var(--text-muted,#abacb2);--c-link:var(--text-link,#76aff6);
--c-line:var(--border-subtle,rgba(151,151,159,.12));--c-line2:var(--border-normal,rgba(151,151,159,.2));--c-mod:var(--background-mod-subtle,rgba(151,151,159,.12));--c-mod2:var(--background-mod-normal,rgba(151,151,159,.16));
--c-hover:var(--interactive-background-hover,rgba(151,151,159,.12));--c-selected:var(--interactive-background-selected,rgba(151,151,159,.2));--c-item:var(--interactive-text-default,#c5c6ca);--c-item-hi:var(--interactive-text-active,#fff);
--c-brand:var(--brand-500,#5865f2);--c-ok:var(--status-positive,#23a55a);--c-warn:var(--status-warning,#f0b232);--c-bad:var(--status-danger,#f23f43);--c-orange:color-mix(in srgb,var(--c-warn) 45%,var(--c-bad));
--c-ok-text:var(--text-feedback-positive,#7ecb94);--c-warn-text:var(--text-feedback-warning,#fcb529);--c-bad-text:var(--text-feedback-critical,#ffa09b);--c-info-text:var(--text-feedback-info,#8ebefa);--c-focus:var(--border-focus,#6aa8f4);
--r-xs:var(--radius-xs,4px);--r-sm:var(--radius-sm,8px);--r-md:var(--radius-md,12px);--f-code:var(--font-code,"gg mono","Source Code Pro",Consolas,monospace);
position:fixed;inset:0;z-index:2147483000;display:flex;background:var(--c-bg);color:var(--c-text);font:14px/1.43 var(--font-primary,"gg sans","Noto Sans","Helvetica Neue",Helvetica,Arial,sans-serif);-webkit-font-smoothing:antialiased;text-align:left}
${R} *{box-sizing:border-box}
${R} [hidden]{display:none!important}
${R} :focus-visible{outline:2px solid var(--c-focus);outline-offset:2px}
${R} .sr{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0);white-space:nowrap}
${R} .ico{width:16px;height:16px;flex:none;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round}
${R} p{margin:0}
${R} a{color:var(--c-link);text-decoration:none}
${R} a:hover{text-decoration:underline}
${R} .mono{font-family:var(--f-code);font-size:13px}
${R} .muted{color:var(--c-muted)}
${R} .subtle{color:var(--c-subtle)}
${R} .note{margin-top:4px;font-size:12px;line-height:16px;color:var(--c-muted)}
${R} .side{flex:1 0 218px;display:flex;justify-content:flex-end;background:var(--c-side);overflow-y:auto;scrollbar-width:thin}
${R} .side-in{display:flex;flex-direction:column;width:218px;padding:60px 6px 60px 20px}
${R} .side-head{padding:6px 10px;font-size:12px;line-height:16px;font-weight:700;text-transform:uppercase;letter-spacing:.02em;color:var(--c-muted)}
${R} .item{display:flex;align-items:center;gap:8px;width:100%;min-height:32px;margin-bottom:2px;padding:6px 10px;border:0;border-radius:var(--r-xs);background:none;color:var(--c-item);font:500 16px/20px var(--font-primary,"gg sans",sans-serif);text-align:left;cursor:pointer}
${R} .item:hover{background:var(--c-hover);color:var(--interactive-text-hover,#fff)}
${R} .item[aria-selected="true"]{background:var(--c-selected);color:var(--c-item-hi)}
${R} .item:disabled{opacity:.5;cursor:not-allowed;background:none}
${R} .item .count{margin-left:auto;min-width:16px;height:16px;padding:0 4px;border-radius:8px;background:var(--c-bad);color:#fff;font-size:12px;line-height:16px;font-weight:700;text-align:center}
${R} .side .sep{height:1px;margin:8px 10px;background:var(--c-line)}
${R} .who{display:flex;align-items:center;gap:10px;padding:6px 10px 10px;min-width:0}
${R} .avatar{position:relative;flex:none;width:32px;height:32px}
${R} .avatar img,${R} .avatar .ini{width:100%;height:100%;border-radius:50%;object-fit:cover}
${R} .avatar .ini{display:grid;place-items:center;background:var(--c-brand);color:#fff;font-weight:600}
${R} .avatar img{position:absolute;inset:0}
${R} .avatar .pres{position:absolute;right:-3px;bottom:-3px;width:14px;height:14px;border-radius:50%;border:3px solid var(--c-side);background:var(--c-ok)}
${R} .avatar.suspended .pres{background:var(--c-bad)}
${R} .who .name{font-size:14px;line-height:18px;font-weight:600;color:var(--c-strong);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
${R} .who .mode{font-size:12px;line-height:16px;color:var(--c-muted)}
${R} .content{flex:1 1 800px;min-width:0;background:var(--c-bg);overflow-x:hidden;overflow-y:auto}
${R} .content-row{display:flex;align-items:flex-start;min-height:100%}
${R} .col{flex:1 1 auto;min-width:0;max-width:740px;padding:60px 40px 80px}
${R} .tools{position:sticky;top:0;flex:0 0 36px;margin-right:21px;padding-top:60px}
${R} .close-btn{display:grid;place-items:center;width:36px;height:36px;padding:0;border:2px solid var(--c-item);border-radius:50%;background:none;color:var(--c-item);cursor:pointer}
${R} .close-btn:hover{background:var(--c-hover);border-color:var(--c-item-hi);color:var(--c-item-hi)}
${R} .close-btn .ico{width:18px;height:18px}
${R} .esc{margin-top:8px;text-align:center;font-size:13px;line-height:16px;font-weight:600;color:var(--c-muted)}
${R} h2.title{margin:0 0 8px;font-size:20px;line-height:24px;font-weight:600;color:var(--c-strong)}
${R} .lede{margin:0 0 20px;color:var(--c-subtle)}
${R} .lede b{font-weight:600;color:var(--c-strong)}
${R} .pad{padding:16px}
${R} .r .rv .btn+.btn{margin-left:4px}
${R} .group+.note,${R} .stack+.note{margin-top:8px}
${R} h3.eyebrow{margin:0 0 8px;font-size:12px;line-height:16px;font-weight:700;text-transform:uppercase;letter-spacing:.02em;color:var(--c-muted)}
${R} .divider{height:1px;margin:32px 0;background:var(--c-line)}
${R} .stack>*+*{margin-top:8px}
${R} .group{border-radius:var(--r-sm);background:var(--c-card);overflow:hidden}
${R} .r{display:flex;align-items:center;gap:16px;min-height:48px;padding:12px 16px}
${R} .r+.r{border-top:1px solid var(--c-line)}
${R} .r .rl{flex:1;min-width:0}
${R} .r .rt{font-size:14px;line-height:18px;font-weight:500;color:var(--c-text);overflow-wrap:anywhere}
${R} .r .rv{flex:none;max-width:55%;text-align:right;color:var(--c-subtle);font-variant-numeric:tabular-nums;overflow-wrap:anywhere}
${R} .standing{padding:16px 16px 20px;border-radius:var(--r-sm);background:var(--c-card)}
${R} .standing-head{display:flex;align-items:center;gap:12px}
${R} .standing-head .avatar{width:40px;height:40px}
${R} .standing-head .avatar .pres{display:none}
${R} .standing h4{margin:0;font-size:16px;line-height:20px;font-weight:600;color:var(--c-strong)}
${R} .standing .desc{margin-top:2px;color:var(--c-subtle)}
${R} .track{position:relative;display:grid;grid-template-columns:repeat(5,minmax(0,1fr));margin:24px 0 0;padding:0;list-style:none}
${R} .track::before{content:"";position:absolute;top:7px;left:10%;right:10%;height:2px;background:var(--c-line2)}
${R} .step{position:relative;display:flex;flex-direction:column;align-items:center;gap:8px;min-width:0;text-align:center;font-size:12px;line-height:16px;font-weight:500;color:var(--c-muted)}
${R} .step .dot{width:12px;height:12px;margin-top:2px;border-radius:50%;background:var(--c-card);border:2px solid var(--c-line2)}
${R} .step.cur{color:var(--tone);font-weight:600}
${R} .step.cur .dot{width:16px;height:16px;margin-top:0;border:0;background:var(--tone);box-shadow:0 0 0 4px var(--c-card)}
${R} .tone-ok{--tone:var(--c-ok)}${R} .tone-warn{--tone:var(--c-warn)}${R} .tone-orange{--tone:var(--c-orange)}${R} .tone-bad{--tone:var(--c-bad)}${R} .tone-muted{--tone:var(--c-muted)}
${R} .btn{display:inline-flex;align-items:center;justify-content:center;gap:6px;min-height:38px;padding:2px 16px;border:1px solid var(--control-secondary-border-default,transparent);border-radius:var(--r-sm);background:var(--control-secondary-background-default,rgba(255,255,255,.08));color:var(--control-secondary-text-default,#fff);font:500 14px/16px var(--font-primary,"gg sans",sans-serif);cursor:pointer;transition:background-color .17s ease}
${R} .btn:hover:not(:disabled){background:var(--control-secondary-background-hover,rgba(255,255,255,.16))}
${R} .btn.primary{border-color:transparent;background:var(--control-primary-background-default,#5865f2);color:var(--control-primary-text-default,#fff)}
${R} .btn.primary:hover:not(:disabled){background:var(--control-primary-background-hover,#4752c4)}
${R} .btn.danger{border-color:transparent;background:var(--control-critical-primary-background-default,#d22d39);color:#fff}
${R} .btn.danger:hover:not(:disabled){background:var(--control-critical-primary-background-hover,#a9232e)}
${R} .btn.sm{min-height:32px;padding:2px 12px}
${R} .btn.link{min-height:0;padding:0;border:0;background:none;color:var(--c-link)}
${R} .btn.link:hover:not(:disabled){background:none;text-decoration:underline}
${R} .btn:disabled{opacity:.5;cursor:not-allowed}
${R} .btn .ico{width:16px;height:16px}
${R} .btns{display:flex;flex-wrap:wrap;align-items:center;gap:8px;margin-top:16px}
${R} .tag{display:inline-flex;align-items:center;gap:4px;height:20px;padding:0 6px;border-radius:var(--r-xs);background:var(--c-mod2);color:var(--c-subtle);font-size:12px;line-height:16px;font-weight:600;white-space:nowrap}
${R} .tag .ico{width:12px;height:12px}
${R} .tag.ok{background:color-mix(in srgb,var(--c-ok) 18%,transparent);color:var(--c-ok-text)}
${R} .tag.warn{background:color-mix(in srgb,var(--c-warn) 18%,transparent);color:var(--c-warn-text)}
${R} .tag.bad{background:color-mix(in srgb,var(--c-bad) 18%,transparent);color:var(--c-bad-text)}
${R} .tag.brand{background:var(--c-brand);color:#fff}
${R} .tag.m{justify-content:center;min-width:54px;font-family:var(--f-code);font-size:11px}
${R} .tag.m.get{background:color-mix(in srgb,var(--c-ok) 18%,transparent);color:var(--c-ok-text)}
${R} .tag.m.post{background:color-mix(in srgb,var(--c-warn) 18%,transparent);color:var(--c-warn-text)}
${R} .tag.m.put,${R} .tag.m.patch{background:color-mix(in srgb,var(--c-brand) 25%,transparent);color:var(--c-info-text)}
${R} .tag.m.delete{background:color-mix(in srgb,var(--c-bad) 18%,transparent);color:var(--c-bad-text)}
${R} .yes,${R} .no,${R} .part{display:inline-flex;align-items:center;gap:6px;font-weight:500}
${R} .yes{color:var(--c-ok-text)}${R} .no{color:var(--c-muted)}${R} .part{color:var(--c-warn-text)}
${R} .notice{display:flex;align-items:flex-start;gap:12px;padding:12px;border:1px solid;border-radius:var(--r-sm);color:var(--c-text);line-height:18px}
${R} .notice>.ico{width:20px;height:20px}
${R} .notice.info{border-color:var(--border-feedback-info,#8ebefa);background:var(--background-feedback-info,#0074e314)}
${R} .notice.info>.ico{color:var(--c-info-text)}
${R} .notice.warn{border-color:var(--border-feedback-warning,#fcb529);background:var(--background-feedback-warning,#f8a30014)}
${R} .notice.warn>.ico{color:var(--c-warn-text)}
${R} .notice.bad{border-color:var(--border-feedback-critical,#ffa09b);background:var(--background-feedback-critical,#d22d3914)}
${R} .notice.bad>.ico{color:var(--c-bad-text)}
${R} .notice b{color:var(--c-strong);font-weight:600}
${R} .notice .btn.link{font-weight:500}
${R} details.vio{border-radius:var(--r-sm);background:var(--c-card);overflow:hidden}
${R} details.vio+details.vio{margin-top:8px}
${R} .vio>summary{display:flex;align-items:center;gap:12px;padding:12px 16px;list-style:none;cursor:pointer}
${R} .vio>summary::-webkit-details-marker{display:none}
${R} .vio>summary:hover{background:var(--c-hover)}
${R} .vio .state{display:grid;place-items:center;flex:none;width:32px;height:32px;border-radius:50%;background:color-mix(in srgb,var(--tone) 18%,transparent);color:var(--tone)}
${R} .vio-main{flex:1;min-width:0}
${R} .vio-title{font-size:16px;line-height:20px;font-weight:600;color:var(--c-strong);overflow-wrap:anywhere}
${R} .vio-sub{margin-top:2px;color:var(--c-muted);overflow-wrap:anywhere}
${R} .vio .tags{display:flex;flex-wrap:wrap;justify-content:flex-end;gap:4px}
${R} .chev{flex:none;color:var(--c-muted);transition:transform .15s ease}
${R} details[open]>summary .chev{transform:rotate(180deg)}
${R} .vio-body{padding:4px 16px 16px;border-top:1px solid var(--c-line)}
${R} .kv{display:grid;grid-template-columns:minmax(120px,160px) minmax(0,1fr);gap:0 16px}
${R} .kv>div{padding:10px 0;border-top:1px solid var(--c-line);min-width:0;overflow-wrap:anywhere}
${R} .kv>div:nth-child(-n+2){border-top:0}
${R} .kv>.k{font-weight:500;color:var(--c-subtle)}
${R} .act+.act{margin-top:8px}
${R} .act b{font-weight:600;color:var(--c-strong)}
${R} .guild{display:flex;align-items:center;gap:12px;margin-top:12px;padding:12px;border-radius:var(--r-sm);background:var(--c-mod)}
${R} .gicon{display:grid;place-items:center;flex:none;width:40px;height:40px;border-radius:var(--r-md);background:var(--c-brand);color:#fff;font-weight:600}
${R} .appeal{margin-top:16px;padding-top:16px;border-top:1px solid var(--c-line)}
${R} .radios{display:grid;gap:8px;margin:0;padding:0;border:0}
${R} .radio{display:flex;align-items:center;gap:12px;padding:10px 12px;border-radius:var(--r-sm);background:var(--c-mod);color:var(--c-text);font-weight:500;cursor:pointer}
${R} .radio:hover{background:var(--c-mod2)}
${R} .radio:has(input:checked){background:var(--c-selected)}
${R} .radio input{appearance:none;display:grid;place-items:center;flex:none;width:20px;height:20px;margin:0;border:2px solid var(--c-item);border-radius:50%;background:none;cursor:pointer}
${R} .radio input:checked{border-color:var(--c-brand)}
${R} .radio input:checked::before{content:"";width:10px;height:10px;border-radius:50%;background:var(--c-brand)}
${R} label.fl,${R} legend.fl{display:block;margin:0 0 8px;padding:0;font-size:12px;line-height:16px;font-weight:700;text-transform:uppercase;letter-spacing:.02em;color:var(--c-muted)}
${R} .field{margin-top:16px}
${R} select,${R} textarea,${R} input:not([type=radio]){width:100%;min-height:40px;padding:10px;border:1px solid var(--input-border-default,rgba(151,151,159,.2));border-radius:var(--r-sm);background:var(--input-background-default,rgba(0,0,0,.08));color:var(--input-text-default,var(--c-text));font:inherit;font-size:14px}
${R} select:hover,${R} textarea:hover,${R} input:not([type=radio]):hover{border-color:var(--input-border-hover,rgba(151,151,159,.28))}
${R} select:focus,${R} textarea:focus,${R} input:not([type=radio]):focus{outline:none;border-color:var(--c-focus)}
${R} select:disabled,${R} input:disabled{opacity:.5;cursor:not-allowed}
${R} textarea{min-height:88px;resize:vertical;line-height:1.43}
${R} .grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:16px}
${R} .runner{display:grid;grid-template-columns:120px minmax(0,1fr);gap:16px}
${R} pre{margin:8px 0 0;padding:12px;border:1px solid var(--c-line);border-radius:var(--r-sm);background:var(--c-code);color:var(--c-subtle);font:13px/18px var(--f-code);white-space:pre;overflow:auto;max-height:320px}
${R} .res{margin-top:12px}
${R} .res-h{display:flex;align-items:center;gap:6px;font-weight:600}
${R} .res.ok .res-h{color:var(--c-ok-text)}${R} .res.bad .res-h{color:var(--c-bad-text)}${R} .res.pending .res-h{color:var(--c-muted)}
${R} .res.pending .ico{animation:shx-spin 1s linear infinite}
${R} details.raw{margin-top:12px}
${R} details.raw>summary{width:max-content;color:var(--c-link);cursor:pointer}
${R} .tbl{position:relative;overflow-x:auto;border-radius:var(--r-sm);background:var(--c-card)}
${R} table{width:100%;min-width:620px;border-collapse:collapse}
${R} th,${R} td{padding:12px 16px;border-top:1px solid var(--c-line);text-align:left;vertical-align:top}
${R} thead th{border-top:0;font-size:12px;line-height:16px;font-weight:700;text-transform:uppercase;letter-spacing:.02em;color:var(--c-muted);white-space:nowrap}
${R} thead th .tag{margin-left:6px;text-transform:none;letter-spacing:0}
${R} tbody th{width:30%;font-weight:500;color:var(--c-text)}
${R} .here{background:var(--c-mod)}
${R} .ep{display:block;margin-top:4px;color:var(--c-muted);font-family:var(--f-code);font-size:12px;line-height:16px;overflow-wrap:break-word}
${R} .ep.plain{font-family:inherit}
${R} .nw{white-space:nowrap}
${R} .eplabel{margin:20px 0 8px;font-size:12px;line-height:16px;font-weight:600;color:var(--c-subtle)}
${R} .eplabel:first-child{margin-top:12px}
${R} .eprow{display:flex;align-items:center;gap:12px;padding:10px 16px}
${R} .eprow+.eprow{border-top:1px solid var(--c-line)}
${R} .eprow .path{flex:1;min-width:0;overflow-wrap:anywhere}
${R} .eprow.dim>:not(.btn){opacity:.55}
${R} .linkrow{display:flex;align-items:center;gap:12px;padding:12px 16px;color:var(--c-text);font-weight:500}
${R} .linkrow+.linkrow{border-top:1px solid var(--c-line)}
${R} .linkrow:hover{background:var(--c-hover);text-decoration:none}
${R} .linkrow .ico{margin-left:auto;color:var(--c-muted)}
${R} .empty{padding:40px 16px;border-radius:var(--r-sm);background:var(--c-card);text-align:center}
${R} .empty .big{display:grid;place-items:center;width:56px;height:56px;margin:0 auto 16px;border-radius:50%;background:color-mix(in srgb,var(--c-ok) 18%,transparent);color:var(--c-ok)}
${R} .empty .big .ico{width:28px;height:28px}
${R} .empty h4{margin:0 0 4px;font-size:16px;line-height:20px;font-weight:600;color:var(--c-strong)}
${R} .sk{border-radius:var(--r-sm);background:var(--c-card);animation:shx-pulse 1.4s ease-in-out infinite}
@keyframes shx-spin{to{transform:rotate(360deg)}}
@keyframes shx-pulse{50%{opacity:.55}}
@keyframes shx-fade{from{opacity:0}to{opacity:1}}
#shx-captcha{position:fixed;inset:0;z-index:2147483646;display:flex;align-items:center;justify-content:center;padding:16px;background:rgba(0,0,0,.7);color:var(--text-default,#f3f3f4);font:14px/1.43 var(--font-primary,"gg sans","Noto Sans",Helvetica,Arial,sans-serif);overflow:auto}
#shx-captcha *{box-sizing:border-box}
#shx-captcha .shx-captcha-panel{width:440px;max-width:100%;max-height:calc(100dvh - 32px);overflow:auto;padding:24px;border:1px solid var(--border-subtle,rgba(151,151,159,.12));border-radius:var(--radius-md,12px);background:var(--background-surface-high,#393a41);box-shadow:0 8px 24px rgba(0,0,0,.4);animation:shx-fade .16s ease-out}
#shx-captcha h2{margin:0 0 8px;font-size:20px;line-height:24px;font-weight:600;color:var(--text-strong,#fff)}
#shx-captcha p{margin:0 0 12px;color:var(--text-subtle,#c5c6ca)}
#shx-captcha .shx-captcha-request{padding:8px 10px;border-radius:var(--radius-sm,8px);background:var(--background-base-lowest,#2c2d32);color:var(--text-subtle,#c5c6ca);font:13px/18px var(--font-code,"gg mono",Consolas,monospace);overflow-wrap:anywhere}
#shx-captcha-status{min-height:20px;color:var(--text-feedback-info,#8ebefa)!important}
#shx-captcha .shx-captcha-widget{display:flex;justify-content:center;min-height:80px;margin:16px 0}
#shx-captcha .shx-captcha-actions{display:flex;justify-content:flex-end;flex-wrap:wrap;gap:8px}
#shx-captcha button{min-height:38px;padding:2px 16px;border:0;border-radius:var(--radius-sm,8px);background:var(--control-secondary-background-default,rgba(255,255,255,.08));color:var(--control-secondary-text-default,#fff);font:500 14px/16px var(--font-primary,"gg sans",sans-serif);cursor:pointer}
#shx-captcha button:hover{background:var(--control-secondary-background-hover,rgba(255,255,255,.16))}
#shx-captcha button:focus-visible{outline:2px solid var(--border-focus,#6aa8f4);outline-offset:2px}
#shx-captcha button[hidden]{display:none}
@media(max-width:760px){
${R}{flex-direction:column}
${R} .side{flex:none;display:block;overflow:visible}
${R} .side-in{width:auto;padding:12px 60px 0 16px}
${R} .side-head,${R} .side .sep{display:none}
${R} .who{order:-1;padding:0 0 10px}
${R} [role=tablist],${R} .side-actions{display:flex;flex-wrap:wrap;gap:4px;padding-bottom:8px}
${R} .item{width:auto;margin:0;white-space:nowrap;font-size:14px}
${R} .content{flex:1 1 auto}
${R} .col{padding:20px 16px 60px}
${R} .tools{position:fixed;top:12px;right:12px;z-index:2;margin:0;padding:0}
${R} .esc{display:none}
${R} .kv{grid-template-columns:1fr}${R} .kv>.v{border-top:0;padding-top:0}${R} .kv>div:nth-child(2){padding-top:0}
${R} .vio>summary{flex-wrap:wrap}${R} .vio .tags{order:3;width:100%;justify-content:flex-start;padding-left:44px}
${R} .runner{grid-template-columns:1fr}
${R} .r{flex-wrap:wrap}${R} .r .rv{max-width:none}
${R} .eprow{flex-wrap:wrap}
}
@media(prefers-reduced-motion:reduce){${R} *,#shx-captcha *{animation:none!important;transition:none!important}}
</style><nav class="side" aria-label="Safety Hub"><div class="side-in" id="shx-side"></div></nav>
<div class="content" id="shx-scroll"><div class="content-row"><main class="col" id="shx-body"></main>
<div class="tools"><button type="button" class="close-btn" aria-label="Close">${icon("x")}</button><div class="esc" aria-hidden="true">ESC</div></div></div></div>`;

    const body = () => root.querySelector("#shx-body");
    const scroller = () => root.querySelector("#shx-scroll");
    root.querySelector(".close-btn").onclick = () => dispose();
    root.addEventListener("click", e => {
        const target = e.target.closest?.("[data-goto]");
        if (!target || !root.contains(target)) return;
        e.preventDefault();
        goTab(target.dataset.goto);
    });

    function selectTab(id, focus) {
        if (!TABS.some(([t]) => t === id)) id = "overview";
        state.tab = id;
        for (const tab of root.querySelectorAll('[role="tab"]')) {
            const on = tab.dataset.tab === id;
            tab.setAttribute("aria-selected", String(on));
            tab.tabIndex = on ? 0 : -1;
            if (on && focus) tab.focus();
        }
        for (const panel of root.querySelectorAll('[role="tabpanel"]')) panel.hidden = panel.dataset.tab !== id;
    }
    function goTab(id, focusTab = false) {
        selectTab(id, focusTab);
        scroller().scrollTop = 0;
    }

    function renderSide() {
        const side = root.querySelector("#shx-side");
        const d = state.data;
        const name = d?.username ?? currentUser()?.username ?? null;
        const active = d ? classificationsOf(d).filter(c => !rel(c.max_expiration_time).expired).length : 0;
        const mode = state.suspended ? "Suspended session" : state.authFound ? "Active account" : "Active account (assumed)";
        const tip = state.suspended
            ? "A suspended-user token was found in memory; requests use the suspended endpoints."
            : state.authFound ? "No suspended token: this is a normally signed-in account."
                : "Discord's AuthenticationStore wasn't found, so a suspended token couldn't be checked.";
        side.innerHTML = `<div class="side-head">Safety Hub</div>
            <div role="tablist" aria-label="Safety Hub sections" aria-orientation="vertical">${TABS.map(([id, label]) =>
                `<button type="button" class="item" role="tab" id="shx-tab-${id}" data-tab="${id}" aria-controls="shx-panel-${id}">${esc(label)}${id === "violations" && active
                    ? `<span class="count"><span class="sr">, </span>${active}<span class="sr"> active</span></span>` : ""}</button>`).join("")}</div>
            <div class="sep"></div>
            <div class="side-head">Session</div>
            <div class="who" title="${esc(tip)}">${avatarHTML(32)}<div style="min-width:0"><div class="name">${esc(name ?? "Unknown user")}</div><div class="mode">${esc(mode)}</div></div></div>
            <div class="side-actions">
                <button type="button" class="item" data-act="refresh"${state.busy ? " disabled" : ""}>${state.busy ? "Refreshing…" : "Refresh"}</button>
                <button type="button" class="item" data-act="copy"${d ? "" : " disabled"}>Copy JSON</button>
            </div>`;
        side.querySelector('[data-act="refresh"]').onclick = load;
        const copy = side.querySelector('[data-act="copy"]');
        copy.onclick = () => {
            const say = text => { copy.textContent = text; setTimeout(() => { if (copy.isConnected) copy.textContent = "Copy JSON"; }, 1400); };
            (navigator.clipboard?.writeText(JSON.stringify(state.data, null, 2)) ?? Promise.reject()).then(() => say("Copied"), () => say("Copy failed"));
        };
        const tabs = side.querySelector('[role="tablist"]');
        for (const tab of tabs.children) tab.onclick = () => goTab(tab.dataset.tab);
        tabs.addEventListener("keydown", e => {
            const ids = TABS.map(([id]) => id);
            let i = ids.indexOf(state.tab);
            if (e.key === "ArrowDown" || e.key === "ArrowRight") i = (i + 1) % ids.length;
            else if (e.key === "ArrowUp" || e.key === "ArrowLeft") i = (i - 1 + ids.length) % ids.length;
            else if (e.key === "Home") i = 0;
            else if (e.key === "End") i = ids.length - 1;
            else return;
            e.preventDefault();
            goTab(ids[i], true);
        });
    }

    // Small builders for the settings-page layout. Callers pass already-escaped HTML.
    const head = (title, lede) => [el("h2", "title", esc(title)), el("p", "lede", lede)];
    const eyebrow = text => el("h3", "eyebrow", esc(text));
    const divider = () => el("div", "divider");
    const row = (label, value, note) => `<div class="r"><div class="rl"><div class="rt">${label}</div>${note ? `<div class="note">${note}</div>` : ""}</div><div class="rv">${value}</div></div>`;

    // Loading and error placeholders shared by every data-driven page.
    function waiting() {
        if (state.error) {
            const box = el("div");
            const hint = state.suspended
                ? "The suspended token may have expired. Log in again until the suspension screen appears, then paste the script again."
                : "If this account is suspended, log in until Discord shows the suspension screen, then paste the script again. The suspended token only exists in memory for that session. Otherwise, check that you're signed in and try again.";
            box.innerHTML = `<div class="notice bad">${icon("warn")}<div><b>Couldn't load your Safety Hub record.</b> ${esc(hint)}</div></div>
                <pre>${pretty(state.error)}</pre><div class="btns"><button type="button" class="btn primary">Try again</button></div>`;
            box.querySelector("button").onclick = load;
            return [box];
        }
        if (!state.data) {
            const sk = el("div", "stack", `<span class="sr" role="status">Loading your Safety Hub record…</span><div class="sk" style="height:148px"></div><div class="sk" style="height:240px"></div>`);
            sk.setAttribute("aria-busy", "true");
            return [sk];
        }
        return null;
    }

    function fetchNotice() {
        if (REST) return null;
        const how = state.suspended ? "Suspended endpoints authenticate with the token in the body, so they still work."
            : sessionToken() ? "Requests use your session from Discord's AuthenticationStore." : "No session token was found, so account requests will likely fail.";
        const box = el("div", "notice warn", `${icon("warn")}<div>Discord's REST client wasn't found, so requests fall back to plain fetch. ${esc(how)}</div>`);
        box.style.marginBottom = "20px";
        return box;
    }

    function standingCard(d) {
        const cur = d.account_standing?.state;
        const level = STANDING_LEVELS.find(l => l.state === cur);
        return el("div", `standing tone-${level?.tone ?? "muted"}`, `<div class="standing-head">${avatarHTML(40)}<div>
                <h4>${esc(level?.title ?? "Standing unavailable")}</h4>
                <div class="desc">${esc(level?.text ?? `Discord returned standing ${cur ?? "none"}, which isn't on the known scale.`)}</div></div></div>
            <ol class="track" aria-label="Standing scale">${STANDING_LEVELS.map(l => l.state === cur
                ? `<li class="step cur tone-${l.tone}" aria-current="step"><span class="dot"></span><span>${esc(l.label)}</span></li>`
                : `<li class="step"><span class="dot"></span><span>${esc(l.label)}</span></li>`).join("")}</ol>`);
    }

    function overviewPanel() {
        const out = head("Account Standing", "How Discord currently rates this account, and what is on its record.");
        const fallback = fetchNotice();
        if (fallback) out.push(fallback);
        const wait = waiting();
        if (wait) return [...out, ...wait];
        const d = state.data;
        const user = d.classifications ?? [], guild = d.guild_classifications ?? [];
        const all = [...user, ...guild];
        const active = all.filter(c => !rel(c.max_expiration_time).expired);
        // Two different things: whether an appeal was ever filed, and whether the in-app flow is offered.
        // A spam/COPPA/developer row can have no appeal on record yet still be webform-only.
        const inApp = all.filter(c => appealability(c, d).ok);
        const neverAppealed = all.filter(c => c.appeal_status == null);
        const level = STANDING_LEVELS.find(l => l.state === d.account_standing?.state);

        const top = el("div", "stack");
        top.append(standingCard(d), el("div", "notice info", state.suspended
            ? `${icon("info")}<div><b>Suspended session.</b> This record came from <span class="mono">POST /safety-hub/suspended/@me</span> using the suspended token Discord keeps in memory (${tokenHint()}). This script never saves or logs it. <button type="button" class="btn link" data-goto="capabilities">Compare with an active account</button></div>`
            : `${icon("info")}<div><b>Active account.</b> This record came from <span class="mono">GET /safety-hub/@me</span> using your normal session. <button type="button" class="btn link" data-goto="capabilities">See what an active Safety Hub supports</button></div>`));
        out.push(top, divider(), eyebrow("Summary"));

        const summary = el("div", "stack");
        summary.append(el("div", "group", [
            row("Standing", `${esc(level?.label ?? "Unknown")} <span class="mono muted">${esc(d.account_standing?.state ?? "null")}</span>`),
            row("Personal violations", String(user.length)),
            row("Server violations", String(guild.length)),
            row("Active now", `${active.length} of ${all.length}`),
            row("In-app appeal available", `${inApp.length} of ${all.length}`),
            row("No appeal on record", `${neverAppealed.length} of ${all.length}`),
            row("Appeal eligible", yesNo(d.is_appeal_eligible)),
            row("DSA eligible", yesNo(d.is_dsa_eligible)),
            row("Appeal routes", esc((d.appeal_eligibility ?? []).map(x => APPEAL_ELIGIBILITY[x] ?? x).join(", ")) || "—"),
        ].join("")));
        if (neverAppealed.length) {
            const viaInApp = neverAppealed.filter(c => appealability(c, d).ok).length;
            const tail = viaInApp < neverAppealed.length
                ? ` Only ${viaInApp} of those can use the in-app flow; the rest are routed to a webform (spam, COPPA or developer classifications).`
                : "";
            summary.append(el("div", "notice warn", `${icon("warn")}<div><b>${neverAppealed.length} violation${neverAppealed.length > 1 ? "s have" : " has"} no appeal on record</b>, including any the built-in page hides because their expiry is null or already past.${esc(tail)} <button type="button" class="btn link" data-goto="violations">Review violations</button></div>`));
        }
        out.push(summary, divider(), eyebrow("Server-side flags"));

        const flags = [
            ["expressive_modal_v2_enabled", d.expressive_modal_v2_enabled],
            ["show_expressive_modal_subtitle_alt", d.show_expressive_modal_subtitle_alt],
            ["manual_review_fallback_enabled", d.manual_review_fallback_enabled],
            ["manual_review_decided_underage", d.manual_review_decided_underage],
        ];
        out.push(el("div", "group", flags.map(([k, v]) =>
            row(`<span class="mono">${esc(k)}</span>`, v === true ? tag("true", "warn") : v === undefined ? tag("not returned") : tag(String(v)))).join("")));
        return out;
    }

    function violationsPanel() {
        const out = head("Violations", "Everything Discord returned for this account, including items the built-in page hides because they expired or have no expiry.");
        const wait = waiting();
        if (wait) return [...out, ...wait];
        const d = state.data;
        const user = d.classifications ?? [], guild = d.guild_classifications ?? [];
        if (!user.length && !guild.length) {
            out.push(el("div", "empty", `<div class="big">${icon("shieldCheck")}</div><h4>No violations on record</h4>
                <p class="subtle">Discord returned no personal or server violations for this account.</p>`));
            return out;
        }
        // Active violations first; the sort is stable, so Discord's order holds within each group.
        const ordered = list => [...list].sort((a, b) => rel(a.max_expiration_time).expired - rel(b.max_expiration_time).expired);
        const group = (label, list, isGuild) => {
            const section = el("section");
            section.append(eyebrow(`${label} — ${list.length}`));
            for (const c of ordered(list)) section.append(renderClassification(c, d, isGuild));
            return section;
        };
        if (user.length) out.push(group("Your account", user, false));
        if (user.length && guild.length) out.push(divider());
        if (guild.length) out.push(group("Servers", guild, true));
        return out;
    }

    function renderClassification(c, top, isGuild) {
        const tn = typeName(c.classification_type);
        const [bandName, bandNote] = band(c.classification_type);
        const exp = rel(c.max_expiration_time);
        const ap = appealability(c, top);
        const actions = c.actions ?? [];
        const appealed = c.appeal_status?.status;
        const card = el("details", `vio tone-${exp.expired ? "muted" : appealed === 3 ? "ok" : "bad"}`);
        const when = c.max_expiration_time == null ? "No expiry set" : exp.expired ? `Expired ${exp.text}` : `Expires ${exp.text}`;
        const tags = [
            c.appeal_status ? tag(APPEAL_LABEL[appealed] ?? String(appealed), appealed === 3 ? "ok" : appealed === 1 ? "warn" : "bad") : tag("Not appealed"),
            c.is_spam ? tag("Spam", "warn") : "",
            c.is_coppa ? tag("COPPA", "warn") : "",
            c.is_developer_classification ? tag("Developer", "warn") : "",
        ].join("");
        card.innerHTML = `<summary><span class="state">${icon(exp.expired ? "clock" : appealed === 3 ? "check" : "alert")}</span>
            <div class="vio-main"><div class="vio-title">${esc(c.description ?? "(no description)")}</div>
                <div class="vio-sub">${esc([when, bandName, isGuild ? c.guild_metadata?.name : null].filter(Boolean).join(" · "))}</div></div>
            <div class="tags">${tags}</div>${icon("chevron", "chev")}</summary>`;

        const notes = suffixNotes(tn ?? "");
        const link = c.explainer_link ? safeLink(c.explainer_link) : null;
        const rows = [
            ["Classification", `<span class="mono">${esc(tn ?? "not in any client enum")}</span> <span class="muted">· type ${esc(c.classification_type)}</span>`, bandNote + (notes.length ? " · " + notes.join(" · ") : "")],
            ["Actions", actions.map(a => `<div class="act"><b>${esc(actionName(a.action_type) ?? "Unmapped action")}</b> <span class="mono muted">${esc(a.action_type)}</span>
                ${(a.descriptions ?? []).length ? `<div class="subtle">${a.descriptions.map(esc).join("<br>")}</div>` : ""}<div class="note mono">id ${esc(a.id)}</div></div>`).join("") || `<span class="muted">None</span>`],
            ["Expires", esc(c.max_expiration_time == null ? "No expiry set" : exp.when ?? String(c.max_expiration_time)), c.max_expiration_time == null ? exp.note : `raw ${c.max_expiration_time}`],
            ["Appeal", esc(c.appeal_status ? APPEAL_STATUS[appealed] ?? String(appealed) : "None on record"), `route: ${APPEAL_INGESTION[c.appeal_ingestion_type] ?? c.appeal_ingestion_type ?? "—"}`],
            ["Record id", `<span class="mono">${esc(c.id)}</span>`],
            link ? ["Policy", `<a href="${esc(link)}" target="_blank" rel="noreferrer">${esc(link)}</a>`] : null,
            c.flagged_content?.length ? ["Flagged content", `<pre>${pretty(c.flagged_content)}</pre>`] : null,
        ].filter(Boolean);
        const detail = el("div", "vio-body");
        detail.append(el("div", "kv", rows.map(([k, v, n]) => `<div class="k">${esc(k)}</div><div class="v">${v}${n ? `<div class="note">${esc(n)}</div>` : ""}</div>`).join("")));

        if (isGuild && c.guild_metadata) {
            const gm = c.guild_metadata;
            // guild_metadata carries only name + icon_hash — no guild id — so a CDN icon URL can't be built.
            detail.append(el("div", "guild", `<div class="gicon">${esc((gm.name ?? "?").trim().charAt(0).toUpperCase() || "?")}</div><div><b>${esc(gm.name ?? "unknown server")}</b>
                <div class="note">Your role on record: ${esc(MEMBER_TYPE[gm.member_type] ?? gm.member_type)} (member_type ${esc(gm.member_type)}).
                The built-in page shows ${gm.member_type === 1 ? '"Your server …"' : '"A server you joined, …"'}</div></div>`));
        }

        // Appeal form — works even when the built-in page renders no button for this row.
        const signalName = nextId("sig"), txtId = nextId("txt");
        const path = `${state.suspended ? "/safety-hub/suspended/request-review/" : "/safety-hub/request-review/"}${c.id}`;
        const appeal = el("div", "appeal");
        appeal.innerHTML = `<h3 class="eyebrow">Request a review</h3>
            <p class="subtle">${ap.ok ? "Discord's own rules offer the in-app appeal for this violation." : "Discord's app wouldn't offer this: " + esc(ap.reasons.join("; ")) + ". The endpoint is still available, but it may refuse the request."}</p>
            <fieldset class="radios field"><legend class="fl">Why are you appealing?</legend>${Object.entries(SIGNALS).map(([k, v], i) =>
                `<label class="radio"><input type="radio" name="${signalName}" value="${k}"${i === 0 ? " checked" : ""}>${esc(v)}</label>`).join("")}</fieldset>
            <div class="field"><label class="fl" for="${txtId}">Tell us more</label><textarea id="${txtId}" class="txt" placeholder="Explain why this should be reviewed"></textarea>
                <div class="note">Sent as <span class="mono">user_input</span>. Each violation can only be appealed once.</div></div>
            <div class="btns"><button type="button" class="btn primary go">Submit appeal</button><span class="mono muted">PUT ${esc(path)}</span></div>
            <div class="out" aria-live="polite"></div>`;
        const output = appeal.querySelector(".out");
        appeal.querySelector(".go").addEventListener("click", async ev => {
            const btn = ev.currentTarget;
            const signal = +appeal.querySelector("input[type=radio]:checked").value;
            const text = appeal.querySelector(".txt").value.trim();
            if (!confirm(`Submit a real appeal to Discord for classification ${c.id}?\n\nThis is one-shot — once an appeal is on record you can't file another for it.`)) return;
            btn.disabled = true; output.innerHTML = pending("Submitting");
            try { output.innerHTML = result(true, "Appeal submitted", await api.appeal(c.id, signal, text)); }
            catch (e) { output.innerHTML = failure("Appeal", e); }
            finally { btn.disabled = false; }
        });
        detail.append(appeal, el("details", "raw", `<summary>Raw JSON</summary><pre>${pretty(c)}</pre>`));
        card.append(detail);
        return card;
    }

    // What each kind of session can do. A string names the endpoint; null means not available to that session;
    // { partial } marks something the web client can't do on its own. Endpoints are from Discord's mobile client.
    const CAPABILITIES = [
        { f: "View standing and violations", d: "Standing, personal and server violations, expiry, and enforcement actions.", a: "GET /safety-hub/@me", s: "POST /safety-hub/suspended/@me" },
        { f: "Appeal a violation", d: "Request a review with a reason and an explanation. One appeal per violation.", a: "PUT /safety-hub/request-review/{id}", s: "PUT /safety-hub/suspended/request-review/{id}" },
        { f: "List verification methods", d: "The methods and vendors Discord offers this account.", a: "GET /age-verification/methods (+ /v2)", s: "POST /age-verification/suspended/methods (+ /v2)" },
        { f: "Start age verification", d: "Begin verification with a chosen method and vendor.", a: "POST /age-verification/verify (+ /v2)", s: "POST /safety-hub/suspended/request-verification (+ /v2)" },
        { f: "Verify from a specific violation", d: "Tie the verification to one violation, the unban path for age-flagged suspensions.", a: null, s: "request-verification with from_classification_id" },
        { f: "Check verification status", d: "Read the current result. For a suspension, a pass can return UNBANNED.", a: "GET /users/@me/age-verification/check", s: "POST /safety-hub/suspended/check-verification (+ /v2)" },
        { f: "Request a manual age review", d: "Ask a person to review the age decision.", a: "POST /age-verification/manual-review", s: "POST /age-verification/suspended/manual-review" },
        { f: "Reset age verification", d: "Reset the account's age-verification state.", a: "POST /users/@me/age-verification/reset", s: null },
        { f: "Google Wallet verification", d: "Verify with a digital ID from Google Wallet.", a: { partial: "Android app only" }, s: { partial: "Android app only" } },
        { f: "Webform appeals", d: "Spam, COPPA, developer, and app or bot violations route to support forms.", a: "support.discord.com", s: "support.discord.com" },
        { f: "Request runner", d: "Send your own API calls through Discord's client.", a: "Any API path your session can use", s: "Suspended endpoints with {token} in the body" },
    ];

    // [group, method, path, body template, session that can use it, note, fill "token" with the suspended token?]
    // Bodies match what Discord's mobile client sends (Wumpus-Central/discord-mobile-datamining).
    const ENDPOINTS = [
        ["Safety Hub", "GET", "/safety-hub/@me", null, "active", "Your Safety Hub record"],
        ["Safety Hub", "POST", "/safety-hub/suspended/@me", { token: "" }, "suspended", "Your Safety Hub record"],
        ["Safety Hub", "PUT", "/safety-hub/request-review/{id}", { signal: 0, user_input: "" }, "active", "Appeal a violation"],
        ["Safety Hub", "PUT", "/safety-hub/suspended/request-review/{id}", { signal: 0, user_input: "", token: "" }, "suspended", "Appeal a violation"],
        ["Age verification", "GET", "/age-verification/methods", null, "active", "Verification methods"],
        ["Age verification", "GET", "/age-verification/methods/v2", null, "active", "Verification methods"],
        ["Age verification", "POST", "/age-verification/suspended/methods", { token: "" }, "suspended", "Verification methods"],
        ["Age verification", "POST", "/age-verification/suspended/methods/v2", { token: "" }, "suspended", "Verification methods"],
        ["Age verification", "POST", "/age-verification/verify", { method: 1, vendor: 1 }, "active", "Start verification"],
        ["Age verification", "POST", "/age-verification/verify/v2", { method: 1, vendor: 1 }, "active", "Start verification"],
        ["Age verification", "POST", "/safety-hub/suspended/request-verification", { token: "", from_classification_id: null, method: 1 }, "suspended", "Start verification"],
        ["Age verification", "POST", "/safety-hub/suspended/request-verification/v2", { token: "", method: 1, vendor: 1 }, "suspended", "Start verification"],
        ["Age verification", "GET", "/users/@me/age-verification/check", null, "active", "Verification status"],
        ["Age verification", "POST", "/safety-hub/suspended/check-verification", { token: "" }, "suspended", "Verification status"],
        ["Age verification", "POST", "/safety-hub/suspended/check-verification/v2", { token: "", requested_at: null }, "suspended", "Verification status"],
        ["Age verification", "POST", "/age-verification/manual-review", null, "active", "Manual review"],
        ["Age verification", "POST", "/age-verification/suspended/manual-review", { token: "" }, "suspended", "Manual review"],
        ["Age verification", "POST", "/users/@me/age-verification/reset", null, "active", "Reset age-verification state"],
        ["Age verification", "POST", "/age-verification/incode/interview", null, "active", "Incode flow, after starting verification with Incode"],
        ["Age verification", "POST", "/age-verification/incode/session", null, "active", "Incode flow, after starting verification with Incode"],
        ["Age verification", "POST", "/age-verification/google-wallet/request", {}, "active", "Google Wallet (Android app)"],
        ["Age verification", "POST", "/age-verification/google-wallet/verify", { credential_json: "" }, "active", "Google Wallet (Android app)"],
        ["Age verification", "POST", "/age-verification/google-wallet/suspended/request", { token: "" }, "suspended", "Google Wallet (Android app)"],
        ["Age verification", "POST", "/age-verification/google-wallet/suspended/verify", { token: "", credential_json: "" }, "suspended", "Google Wallet (Android app)"],
        ["Reporting", "GET", "/report/options", null, "active", "In-app reporting"],
        ["Reporting", "POST", "/reports", null, "active", "In-app reporting"],
        ["Reporting", "POST", "/reporting/review", { token: "" }, "any", "DSA second look (token from the notice)", false],
        ["Reporting", "GET", "/reporting/unauthenticated/capabilities", null, "any", "DSA, no login"],
    ];

    function capabilitiesPanel() {
        const here = state.suspended ? "s" : "a";
        const out = head("What's Supported", `Discord serves the Safety Hub to a normally signed-in <b>active account</b> and to a <b>suspended session</b>, which authenticates with a token held in memory while the suspension screen is open. This session is ${state.suspended ? "<b>suspended</b>" : "an <b>active account</b>"}, so the highlighted column applies to you.`);
        const th = (label, on) => `<th scope="col"${on ? ' class="here"' : ""}>${label}${on ? tag("This session", "brand") : ""}</th>`;
        const cell = (v, on, other) => `<td${on ? ' class="here"' : ""}>${v == null
            ? `<span class="no">${icon("minus")}Not available</span><span class="ep plain">${other}</span>`
            : typeof v === "object" ? `<span class="part">${icon("info")}${esc(v.partial)}</span>`
                : `<span class="yes">${icon("check")}Supported</span><span class="ep">${esc(v).replace(/\//g, "/<wbr>")}</span>`}</td>`;
        out.push(el("div", "tbl", `<table><thead><tr><th scope="col">Feature</th>${th("Active account", here === "a")}${th("Suspended session", here === "s")}</tr></thead>
            <tbody>${CAPABILITIES.map(x => `<tr><th scope="row">${esc(x.f)}<div class="note">${esc(x.d)}</div></th>${cell(x.a, here === "a", "Suspended sessions only")}${cell(x.s, here === "s", "Active accounts only")}</tr>`).join("")}</tbody></table>`));

        const d = state.data;
        const rows = [];
        if (d) {
            const all = classificationsOf(d);
            rows.push(row("Violations that can use the in-app appeal", `${all.filter(c => appealability(c, d).ok).length} of ${all.length}`));
            rows.push(row("Appeal eligible", yesNo(d.is_appeal_eligible)));
            rows.push(row("DSA eligible", yesNo(d.is_dsa_eligible)));
        }
        rows.push(row("Age verification", state.suspended ? "List, start, start from a violation, check, manual review" : "List, start, check, manual review"));
        rows.push(row("Requests go through", REST ? "Discord's REST client" : "Plain fetch (REST client not found)"));
        out.push(divider(), eyebrow("Right now on this account"), el("div", "group", rows.join("")));

        out.push(divider(), eyebrow("Other appeal routes"), el("div", "group", Object.entries(WEBFORMS).map(([k, v]) =>
            `<a class="linkrow" href="${esc(v)}" target="_blank" rel="noreferrer">${esc(k)}${icon("external")}</a>`).join("")));
        return out;
    }

    function agePanel() {
        const s = state.suspended;
        const out = head("Age Verification", s
            ? "For an age-flagged suspension, a verification that passes can return <span class=\"mono\">UNBANNED</span>."
            : "List, start, or check age verification for this account, or ask a person to review the age decision.");
        const lock = ' disabled data-locked="1" title="Suspended sessions only"';
        const ids = { meth: nextId("meth"), vend: nextId("vend"), from: nextId("from"), list: nextId("vids") };
        const wrap = el("div");
        wrap.innerHTML = `<h3 class="eyebrow">Status</h3>
            <div class="group">
                ${row("Check verification status", `<button type="button" class="btn sm c1">Check status</button> <button type="button" class="btn sm c2"${s ? "" : lock}>${s ? "" : icon("lock")}Check status (v2)</button>`,
                    `<span class="mono">${s ? "POST /safety-hub/suspended/check-verification (+ /v2)" : "GET /users/@me/age-verification/check"}</span>`)}
                ${row("List verification methods", `<button type="button" class="btn sm m1">List methods</button> <button type="button" class="btn sm m2">List methods (v2)</button>`,
                    `<span class="mono">${s ? "POST /age-verification/suspended/methods (+ /v2)" : "GET /age-verification/methods (+ /v2)"}</span>`)}
            </div>
            <p class="note">Suspension statuses: ${Object.entries(VERIFY_STATUS).map(([k, v]) => `<span class="mono nw">${k} ${v}</span>`).join(" · ")}</p>
            <div class="out out-status" aria-live="polite"></div>
            <div class="divider"></div>
            <h3 class="eyebrow">Start verification</h3>
            <div class="grid">
                <div><label class="fl" for="${ids.meth}">Method</label><select id="${ids.meth}" class="agemethod">${Object.entries(AGE_METHODS).map(([k, v]) => `<option value="${k}">${k} · ${v}</option>`).join("")}</select></div>
                <div><label class="fl" for="${ids.vend}">Vendor</label><select id="${ids.vend}" class="vend">${Object.entries(AGE_VENDORS).map(([k, v]) => `<option value="${k}">${k} · ${v}</option>`).join("")}</select></div>
                <div><label class="fl" for="${ids.from}">From violation</label><input id="${ids.from}" class="fromid mono" list="${ids.list}" placeholder="${s ? "optional, v1 only" : "suspended sessions only"}" autocomplete="off" spellcheck="false"${s ? "" : " disabled"}>
                    <datalist id="${ids.list}">${classificationsOf(state.data).map(c => `<option value="${esc(c.id)}">${esc(c.description ?? typeName(c.classification_type) ?? "")}</option>`).join("")}</datalist></div>
            </div>
            <div class="btns"><button type="button" class="btn primary rq1">Start verification</button><button type="button" class="btn rq2">Start verification (v2)</button></div>
            <p class="note mono">${s ? "POST /safety-hub/suspended/request-verification · v2 adds vendor" : "POST /age-verification/verify · POST /age-verification/verify/v2"}</p>
            <div class="out out-start" aria-live="polite"></div>
            <div class="divider"></div>
            <h3 class="eyebrow">Manual review</h3>
            <div class="group">${row("Ask a person to review the age decision", `<button type="button" class="btn sm mr">Request manual review</button>`,
                `<span class="mono">${s ? "POST /age-verification/suspended/manual-review" : "POST /age-verification/manual-review"}</span>${state.data?.manual_review_fallback_enabled === true ? " · manual_review_fallback_enabled is true on your record" : ""}`)}</div>
            <p class="note">Resetting age verification (<span class="mono">POST /users/@me/age-verification/reset</span>) isn't a button here because it resets your verification state. Load it from Developer if you need it.</p>
            <div class="out out-review" aria-live="polite"></div>`;
        const q = s => wrap.querySelector(s);
        let running = false;
        const run = async (label, fn, output) => {
            if (running) return;
            running = true;
            const buttons = wrap.querySelectorAll("button");
            buttons.forEach(button => button.disabled = true);
            output.innerHTML = pending(label);
            try { output.innerHTML = result(true, `${label} ok`, await fn()); }
            catch (e) { output.innerHTML = failure(label, e); }
            finally { running = false; buttons.forEach(button => button.disabled = button.dataset.locked === "1"); }
        };
        const status = q(".out-status"), start = q(".out-start"), review = q(".out-review");
        const method = () => +q(".agemethod").value, vendor = () => +q(".vend").value;
        q(".c1").onclick = () => run("Check status", api.checkStatus, status);
        q(".c2").onclick = () => run("Check status v2", () => api.checkVerificationV2(), status);
        q(".m1").onclick = () => run("List methods", api.ageMethods, status);
        q(".m2").onclick = () => run("List methods v2", api.ageMethodsV2, status);
        q(".rq1").onclick = () => run("Start verification", () => api.startVerification(method(), vendor(), q(".fromid").value.trim() || null), start);
        q(".rq2").onclick = () => run("Start verification v2", () => api.startVerificationV2(method(), vendor()), start);
        q(".mr").onclick = () => {
            if (!confirm("Ask Discord for a manual age review?\n\nThis sends a real request for a person to review the age decision on this account.")) return;
            run("Manual review request", api.manualReview, review);
        };
        out.push(wrap);
        return out;
    }

    function developerPanel() {
        const out = head("Developer", "Send your own requests through Discord's client, or load a known endpoint. Nothing is sent until you press Send.");
        const ids = { m: nextId("mm"), u: nextId("uu"), b: nextId("bb") };
        const rr = el("div", "group pad");
        rr.innerHTML = `<div class="runner">
                <div><label class="fl" for="${ids.m}">Method</label><select id="${ids.m}" class="mm">${["get", "post", "put", "patch", "del"].map(m => `<option value="${m}">${m === "del" ? "DELETE" : m.toUpperCase()}</option>`).join("")}</select></div>
                <div><label class="fl" for="${ids.u}">Path</label><input id="${ids.u}" class="uu mono" spellcheck="false" autocomplete="off"></div>
            </div>
            <div class="field"><label class="fl" for="${ids.b}">JSON body</label><textarea id="${ids.b}" class="bb mono" spellcheck="false" placeholder="Leave blank to send no body"></textarea>
                <div class="note">${state.suspended ? 'Suspended endpoints need <span class="mono">{"token": …}</span> in the body. Insert token adds yours.' : "Your normal session authenticates these calls. Suspended endpoints won't work because this session has no suspended token."}</div></div>
            <div class="btns"><button type="button" class="btn primary send">Send</button>
                <button type="button" class="btn tok"${state.suspended ? "" : ' disabled title="No suspended token in this session"'}>Insert token</button></div>
            <div class="out" aria-live="polite"></div>`;
        const [mm, uu, bb] = [".mm", ".uu", ".bb"].map(s => rr.querySelector(s));
        [mm.value, uu.value, bb.value] = state.suspended ? ["post", "/safety-hub/suspended/@me", "{}"] : ["get", "/safety-hub/@me", ""];
        const rout = rr.querySelector(".out");
        rr.querySelector(".tok").onclick = () => {
            let o = {}; try { o = JSON.parse(bb.value || "{}"); } catch { }
            if (!o || typeof o !== "object" || Array.isArray(o)) o = {};
            o.token = state.token ?? "(no suspended token in this session)";
            bb.value = JSON.stringify(o, null, 2);
        };
        rr.querySelector(".send").onclick = async () => {
            const button = rr.querySelector(".send");
            if (button.disabled) return;
            const m = mm.value, u = uu.value.trim(), bt = bb.value.trim();
            let b;
            if (bt) {
                try { b = JSON.parse(bt); }
                catch { rout.innerHTML = `<div class="res bad"><div class="res-h">${icon("warn")}The body isn't valid JSON.</div></div>`; bb.focus(); return; }
            }
            button.disabled = true;
            rout.innerHTML = pending("Sending");
            try { rout.innerHTML = result(true, `${m === "del" ? "DELETE" : m.toUpperCase()} ${u}`, await api.raw(m, u, b)); }
            catch (e) { rout.innerHTML = failure("Request", e); }
            finally { button.disabled = false; }
        };
        out.push(eyebrow("Request runner"), rr);

        const mode = state.suspended ? "suspended" : "active";
        const scopes = { active: "Active", suspended: "Suspended", any: "Either" };
        const list = el("div");
        for (const group of [...new Set(ENDPOINTS.map(e => e[0]))]) {
            list.append(el("div", "eplabel", esc(group)), el("div", "group", ENDPOINTS.map(([g, m, p, b, scope, note], i) => {
                if (g !== group) return "";
                const fits = scope === "any" || scope === mode;
                return `<div class="eprow${fits ? "" : " dim"}"><span class="tag m ${m.toLowerCase()}">${m}</span>
                    <div class="path"><span class="mono">${esc(p)}</span>${b ? ` <span class="mono muted">{${esc(Object.keys(b).join(", "))}}</span>` : ""}<div class="note">${esc(note)}</div></div>
                    ${tag(scopes[scope], fits ? "ok" : "")}<button type="button" class="btn sm" data-ep="${i}" aria-label="Load ${m} ${esc(p)}">Load</button></div>`;
            }).join("")));
        }
        list.addEventListener("click", e => {
            const btn = e.target.closest("[data-ep]");
            if (!btn) return;
            const [, m, p, b, , , fill = true] = ENDPOINTS[+btn.dataset.ep];
            mm.value = m === "DELETE" ? "del" : m.toLowerCase();
            uu.value = p;
            const template = b ? { ...b } : null;
            if (template && "token" in template && fill) template.token = state.token ?? "(no suspended token in this session)";
            if (template && "requested_at" in template) template.requested_at = state.verifyRequestedAt ?? null;
            bb.value = template ? JSON.stringify(template, null, 2) : "";
            rout.replaceChildren();
            rr.scrollIntoView({ block: "start", behavior: matchMedia("(prefers-reduced-motion: reduce)").matches ? "auto" : "smooth" });
            uu.focus({ preventScroll: true });
        });
        rr.style.scrollMarginTop = "24px";
        out.push(divider(), eyebrow("Known endpoints"), el("p", "note", "Dimmed rows belong to the other session type. Load copies an endpoint into the runner."), list);
        if (state.data) out.push(divider(), eyebrow("Raw response"), el("details", "raw", `<summary>Show raw JSON</summary><pre>${pretty(state.data)}</pre>`));
        return out;
    }

    function render() {
        if (disposed) return;
        renderSide();
        const panels = { overview: overviewPanel, violations: violationsPanel, capabilities: capabilitiesPanel, age: agePanel, developer: developerPanel };
        body().replaceChildren(...TABS.map(([id]) => {
            const panel = el("section", "panel");
            panel.id = `shx-panel-${id}`;
            panel.dataset.tab = id;
            panel.setAttribute("role", "tabpanel");
            panel.setAttribute("aria-labelledby", `shx-tab-${id}`);
            panel.append(...panels[id]());
            return panel;
        }));
        selectTab(state.tab, false);
        // Fall back to the initial when an avatar can't load (offline, blocked, or outside Discord).
        for (const img of root.querySelectorAll(".avatar img")) {
            if (img.complete && !img.naturalWidth) img.remove();
            else img.addEventListener("error", () => img.remove(), { once: true });
        }
    }

    async function load() {
        if (disposed || state.busy) return;
        state.busy = true; state.error = null;
        render();
        try { state.data = await api.fetchData(); }
        catch (e) { state.error = e?.body ?? { message: String(e?.message ?? e), status: e?.status }; }
        finally { state.busy = false; render(); }
    }

    function dispose() {
        if (disposed) return;
        disposed = true;
        lifetime.abort();
        activeCaptcha?.cancel();
        window.removeEventListener("keydown", onKey, true);
        root.remove();
        if (window.__sh?.close === dispose) delete window.__sh;
    }

    function onKey(e) {
        if (e.key !== "Escape" || disposed) return;
        e.stopImmediatePropagation();
        e.preventDefault();
        if (activeCaptcha) activeCaptcha.cancel();
        else dispose();
    }
    window.addEventListener("keydown", onKey, true);

    document.body.append(root);
    load();

    window.__sh = { state, api, reload: load, close: dispose, show: goTab, CLASSIFICATION_TYPES, ACTION_TYPES };
    console.log(`%c[safety hub]%c loaded in ${state.suspended ? "suspended-session" : "active-account"} mode. window.__sh = { state, api, reload, show, close }`, "color:#5865f2;font-weight:bold", "");
})();
